# HANDOFF_2026-04-03_02.md
Session — Alpha-2 Foundation Complete

Last commit: (paste final hash)
Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100

---

## What Shipped This Session

### 1. Memory Scoring — Stanford/Mem0 Pattern
File: brain/memory/memory.py (371 lines, was 264)
- Heuristic importance scorer: 0.05–0.95 based on content signals
- Child safety memories (Ryleigh/Sloane + safety keywords) score 0.95
- Write gate: "ok"/"thanks" noise blocked at store() — never hits Postgres
- Access tracking: episodic retrieval bumps access_count + last_accessed_at
- Weighted retrieval: 0.3 recency + 0.4 importance + 0.3 relevance (Stanford formula)
- Promotion pipeline now reachable: score >= 0.7 AND access >= 3

### 2. TaskGraph Schema + Migration (012)
File: db/migrations/012_taskgraph_schema.sql
Tables: alpha_task_graphs, alpha_task_steps, alpha_task_events
- Full DAG with depends_on UUID[] edges
- Per-step timeout, retry, checkpoint (JSONB)
- Child safety DB constraint: child graphs MUST be child_safe (CHECK + trigger)
- RLS on both tables matching alpha pattern
- user_id is TEXT (not UUID) — matches alpha convention
- Approval gate columns: approval_required, approval_status, approved_by, approved_at

### 3. TaskGraph Executor (LaunchAgent)
File: brain/tasks/executor.py (724 lines)
- Separate process: com.jarvis.alpha.executor
- Polls every 10s for pending/running graphs
- Bounded parallelism: asyncio.Semaphore(3)
- DAG resolution: find_ready_steps() checks depends_on
- Per-step timeout via asyncio.wait_for
- Retry with max_retries, downstream skip on permanent failure
- Approval gate: blocks graph → needs_approval status
- Crash recovery: picks up running graphs on startup
- JSONB string parsing fix for step input
- SECRETS_FILE env var in plist

### 4. TaskGraph Watchdog (LaunchAgent)
File: brain/tasks/watchdog.py (186 lines)
- Separate process: com.jarvis.alpha.watchdog
- Runs every 5 minutes
- Detects steps running past timeout_seconds → resets or marks stuck
- Writes events to alpha_task_events for Buddy visibility
- Catches orphaned graphs (running but no active steps)

### 5. Control API
File: brain/routes/tasks.py (+120 lines)
- POST /v1/tasks/submit — create graph with steps, resolve dependency indices
- POST /v1/tasks/steps/{id}/approve — approval gateway
- POST /v1/tasks/steps/{id}/deny — deny + fail graph
- POST /v1/tasks/{id}/cancel — cancel running graph
- GET /v1/tasks/pending-approvals — for Approvals page

### 6. Approvals Page (NEW)
File: ui/src/pages/Approvals.tsx (196 lines)
- Pending approvals with approve/deny buttons
- Priority badges, content tier badges, user type badges
- Auto-refresh every 30s
- Wired into router + SECURE nav group

### 7. Security Tab (NEW)
File: ui/src/pages/Security.tsx (251 lines)
- TLS certificates with days remaining (color-coded)
- JWT coverage table (RS256, 30 days, route protection)
- Recent TaskGraphs summary
- LaunchAgents status (5 agents)

### 8. Bug Fixes (Overnight Claude Code)
- #14 costs_today_usd: returns 0.0 instead of null
- #13 Health LaunchAgents: 5-agent section added
- #12 Home Service Map: wired to real mesh data
- #10 Mesh topology: iPhone/Sandbox repositioned, no overlap
- Ruff errors fixed (F841 unused var, E402 mid-file import)

### 9. Infrastructure
- restart_alpha.sh: now reloads executor + watchdog (was brain + buddy only)
- LaunchAgent plists: SECRETS_FILE env var added
- dispatch.py: _normalise_step() bridge for old/new step schemas
- executor.py: json.loads() for JSONB string input from asyncpg

---

## End-to-End Test Result

```
Test JSONB fix | completed | {"response": "The capital of France is Paris."}
```

Full pipeline: submit → executor → DAG resolution → Ollama → response → DB ✅

---

## GitHub Issues Closed

- #14 costs_today_usd null → fixed
- #13 Health LaunchAgent section → fixed
- #12 Home Service Map pending → fixed
- #10 Mesh topology overlap → fixed

## GitHub Issues Remaining

- #9 UniFi WAN + clients stubs (alpha side)
- #5 Forge per-project isolation (forge scope)

---

## Alpha-2 Status: COMPLETE

| Deliverable | Status |
|---|---|
| Memory scoring (Stanford/Mem0) | ✅ |
| Write gate + access tracking | ✅ |
| TaskGraph schema + child safety | ✅ |
| Executor LaunchAgent (semaphore=3) | ✅ |
| Watchdog LaunchAgent | ✅ |
| Control API | ✅ |
| Dispatch → Ollama end-to-end | ✅ |
| Approvals page | ✅ |
| Security tab | ✅ |
| Bug fixes #10, #12, #13, #14 | ✅ |

---

## Next Session Priorities (Alpha-3)

1. Dream Mode — overnight agent using TaskGraph (Gateway LaunchAgent)
2. Morning briefing endpoint
3. Voice UI port from jarvis-core (STT/TTS)
4. WebSocket for Buddy events (replace 10s polling)
5. Errors page (Datadog-style log viewer)
6. Clean up debug artifacts (.claude/worktrees)
7. Remove test graphs from DB

---

## Known Issues Introduced

- Executor/watchdog logs go to stderr (executor_error.log) not stdout — logging config sends to stderr by default
- alpha_task_events RLS is admin-only — may need user-scoped policy for Buddy events page
- Test graphs left in DB (Test DAG, Test dispatch fix, Test dispatch v2, Test debug, Test debug v2, Test JSONB fix) — clean up next session
- psutil not in Brain requirements.txt (installed manually for power sampler)

---

## Quick Reference

| Item | Value |
|---|---|
| Alpha Brain URL | https://jarvis-brain.tail40ed36.ts.net:8186 |
| Alpha UI URL | https://jarvis-endpoint.tail40ed36.ts.net:4100 |
| Alpha DB | jarvis_alpha on Brain Postgres 16 |
| Commit script | bash ~/jarvis-alpha/scripts/jarvisalpha_commit.sh "msg" |
| Pull script | bash ~/jarvis-alpha/scripts/jarvisalpha_pull.sh |
| Restart (all) | bash ~/jarvis-alpha/scripts/restart_alpha.sh |
| Executor log | ~/jarvis-alpha/logs/executor_error.log |
| Watchdog log | ~/jarvis-alpha/logs/watchdog_error.log |
| Executor plist | ~/Library/LaunchAgents/com.jarvis.alpha.executor.plist |
| Watchdog plist | ~/Library/LaunchAgents/com.jarvis.alpha.watchdog.plist |
| TaskGraph schema | ~/jarvis-alpha/db/migrations/012_taskgraph_schema.sql |
| Memory scoring | ~/jarvis-alpha/brain/memory/memory.py |
| Dispatch | ~/jarvis-alpha/brain/tasks/dispatch.py |
| Executor | ~/jarvis-alpha/brain/tasks/executor.py |
| Watchdog | ~/jarvis-alpha/brain/tasks/watchdog.py |
| Tasks routes | ~/jarvis-alpha/brain/routes/tasks.py |
| Approvals page | ~/jarvis-alpha/ui/src/pages/Approvals.tsx |
| Security page | ~/jarvis-alpha/ui/src/pages/Security.tsx |
| Secrets (Brain) | ~/jarvis/.secrets |
| SECRETS_FILE env | Set in executor + watchdog plists |
