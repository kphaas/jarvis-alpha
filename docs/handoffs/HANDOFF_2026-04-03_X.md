# HANDOFF_2026-04-03_X.md
Session X — Cost Center v2 + Power Tracking Infrastructure

Last commit: 5d35343
Previous session commit: 0d3402a (Session W)
Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100
Alpha DB: jarvis_alpha on Brain Postgres 16

---

## What Shipped This Session

### 1. Cost Center v1 — Migration 009
File: `brain/db/migrations/009_cost_center.sql`
Tables created:
- `alpha_subscriptions` — subscription tracking (name, URL, cost, billing, renewal)
- `alpha_credit_balance` — Anthropic credit manual entry
- `alpha_power_config` — editable $/kWh rate (seeded at $0.13)

Seeded subscriptions:
- Claude Max — $100.00/mo, monthly, renews 2026-05-01
- Cursor — $20.00/mo, monthly, renews 2026-05-01
- Perplexity Pro — $200.00/yr ($16.67/mo), yearly, renews 2027-01-01

### 2. Cost Center v2 — Migration 010
File: `brain/db/migrations/010_cost_tracking.sql`
Tables created:
- `alpha_cloud_costs` — per-call cloud API logging (provider, model, tokens, cost, session_type, key_name)
- `alpha_budget_config` — per-provider monthly limits (Anthropic $50, Gemini $20, Perplexity $10)
- `alpha_hardware_config` — node hardware + 4yr depreciation (Brain $52.08/mo, Gateway/Endpoint $20.83/mo, Sandbox $12.50/mo)
- `alpha_perplexity_credit` — manual Perplexity credit (seeded $4.84 balance, $0.13 spent)

### 3. Power Tracking Infrastructure — Migration 011
File: `brain/db/migrations/011_power_tracking.sql`
Tables created:
- `alpha_power_readings` — raw 15-min samples, purged after 30 days
- `alpha_power_hourly` — hourly averages, kept 12 months
- `alpha_power_daily` — daily averages, kept 5 years

Rollup strategy: Buddy agent will aggregate raw → hourly → daily (not yet wired to Buddy).

### 4. Brain Routes
File: `brain/routes/costs.py` (rewritten — 1040 lines)
New endpoints:
- GET/POST `/v1/costs/budget` — per-provider limits, editable inline
- GET `/v1/costs/outcomes` — cost per session_type (ask/overnight/forge)
- GET `/v1/costs/hardware` — node TCO with depreciation
- GET/POST `/v1/costs/perplexity` — Perplexity credit manual tracking
- GET `/v1/costs/summary` — expanded: true TCO, hardware, API by provider, outcomes, savings

File: `brain/routes/metrics.py` (new — 172 lines)
New endpoints:
- POST `/v1/metrics/power` — ingest power reading (no auth — internal Tailscale)
- GET `/v1/metrics/power/current` — 24hr rolling average per node
- GET `/v1/metrics/power/history` — 30-day daily history per node
- POST `/v1/metrics/power/rollup` — aggregate raw → hourly → daily, purge old raw

### 5. JWT Skip Paths Updated
File: `brain/middleware/jwt_auth.py`
Added to SKIP_PATHS:
- `/v1/metrics/power`
- `/v1/metrics/power/rollup`

### 6. External API Credentials — Gateway
Secrets added to `~/jarvis/.secrets` on Gateway:
- `ANTHROPIC_ADMIN_KEY` — Anthropic org billing API
- `GCP_BILLING_ACCOUNT_ID` — Google Cloud billing account
- `GCP_BILLING_KEY_PATH` — `/Users/infranet/jarvis/.gcp_billing.json`
- `google-auth==2.38.0` installed in Brain venv

GCP setup completed:
- Service account: `jarvis-billing-reader@jarvis-ai-491617.iam.gserviceaccount.com`
- Role: Billing Account Viewer on jarvis-ai project
- JSON key secured at `~/jarvis/.gcp_billing.json` (chmod 600)
- Org policy re-enabled after key creation

### 7. Power Sampler — All 4 Nodes
Files created:
- `scripts/power_sampler.py` — 121 lines
- `scripts/power_sampler_brain.plist`
- `scripts/power_sampler_gateway.plist`
- `scripts/power_sampler_endpoint.plist`
- `scripts/power_sampler_sandbox.plist`
- `scripts/install_power_sampler.sh`

Method per node:
- Brain: `sudo powermetrics` — real silicon power (confirmed 0.14-0.15W idle CPU package)
- Gateway/Endpoint/Sandbox: `psutil` CPU% → TDP-estimated watts

Sampling interval: 15 minutes
LaunchAgent installed on: Brain ✅, Gateway ✅, Endpoint ✅, Sandbox ✅

Brain live data confirmed:
```json
{
  "node_name": "Brain",
  "avg_watts_24h": 0.145,
  "sample_count": 2,
  "has_live_data": true,
  "source": "live"
}
```
Gateway/Endpoint/Sandbox: first sample pending (15-min cycle, installed end of session).

### 8. Pull Script Fix
File: `scripts/jarvisalpha_pull.sh`
Fixed: secrets path detection — checks `~/jarvis/.secrets` first, falls back to `~/.secrets`
Reason: Sandbox uses `~/.secrets` not `~/jarvis/.secrets`

### 9. CostCenter.tsx — Full Redesign (1591 → 1871 lines)
Sections:
1. Hero — ESTIMATED MONTHLY ($246.36) + CLOUD CREDITS (Anthropic/Gemini/Perplexity balances)
2. Month progress bar — Day X of 30
3. Dual pie charts — ALPHA vs FORGE
4. Cloud API Spend — Anthropic (jarvis_core/jarvis_forge split), Gemini (GCP FREE TIER), Perplexity
5. Cost Per Outcome — Ask/Overnight/Forge (no data yet — populates as calls are logged)
6. Subscriptions — table + 2026 cash view bar chart side by side
7. Power + Hardware (True TCO) — table + LIVE POWER DRAW SVG bar chart
8. Forge Pipeline Costs — link updated to https://jarvis-forge.tail40ed36.ts.net:5001
- Refresh button top right — manual + auto every 1 hour
- All sections fail independently

---

## Current System State

### Services
| Service | Node | Port | Status |
|---|---|---|---|
| FastAPI Alpha Brain | Brain | 8186 | RUNNING (restarted this session) |
| FastAPI Alpha Gateway | Gateway | 8283 | RUNNING |
| nginx Alpha UI | Endpoint | 4100 | RUNNING |
| Postgres jarvis_alpha | Brain | 5432 | RUNNING, migrations 001-011 applied |
| Power Sampler | Brain | LaunchAgent | RUNNING — live data flowing |
| Power Sampler | Gateway | LaunchAgent | INSTALLED — first sample pending |
| Power Sampler | Endpoint | LaunchAgent | INSTALLED — first sample pending |
| Power Sampler | Sandbox | LaunchAgent | INSTALLED — first sample pending |
| jarvis-core Brain | Brain | 8182 | STILL RUNNING — not yet decommissioned |
| jarvis-core Gateway | Gateway | 8282 | STILL RUNNING — not yet decommissioned |

### Key Config
- ANTHROPIC_ADMIN_KEY: Gateway `~/jarvis/.secrets` ✅
- GCP_BILLING_ACCOUNT_ID + KEY_PATH: Gateway `~/jarvis/.secrets` ✅
- google-auth: installed in Brain venv ✅
- Power sampler: all 4 LaunchAgents running ✅
- JWT skip paths include `/v1/metrics/power` ✅

---

## Known Issues Carried Forward

| # | Issue | Priority |
|---|---|---|
| FI-2 | Intel refresh JSONDecodeError in commit script | P2 |
| AI-1 | UDM Pro API stubs — wire real WAN/clients data | P1 |
| AI-2 | Mesh topology visual — Sandbox/iPhone overlap | P2 |
| AI-3 | nginx alpha.conf not in git | P1 |
| AI-6 | Child profile RLS — DB layer only | P1 |
| AI-9 | Health page LaunchAgent section not built | P2 |
| AI-10 | Home Service Map — shows "pending" | P2 |
| Power rollup | Buddy agent not yet wired to rollup task | P2 |
| Anthropic API | Admin API returning $0 — no Alpha cloud calls logged yet | Expected |
| Gemini GCP | GCP billing API fallback to internal — $0 until calls logged | Expected |

---

## Next Session Priority Order

1. Wire Buddy agent power rollup task (hourly + daily aggregation)
2. Update CostCenter PowerSection to use `/v1/metrics/power/current` (live 24hr avg)
3. Security tab — TLS cert expiry, JWT coverage, Prompt Shield, UniFi bar
4. Fix intel refresh JSONDecodeError (FI-2)
5. Wire Home Service Map to real node health (AI-10)
6. TaskGraph schema + migration (Alpha-2 foundation)

---

## Key Reference

| Item | Value |
|---|---|
| Alpha Brain URL | https://jarvis-brain.tail40ed36.ts.net:8186 |
| Alpha UI URL | https://jarvis-endpoint.tail40ed36.ts.net:4100 |
| Alpha DB | jarvis_alpha on Brain Postgres 16 |
| Brain venv | ~/jarvis-alpha/.venv (python3.12) |
| Commit script | bash ~/jarvis-alpha/scripts/jarvisalpha_commit.sh "msg" |
| Pull script | bash ~/jarvis-alpha/scripts/jarvisalpha_pull.sh |
| Restart Brain | bash ~/jarvis-alpha/scripts/restart_alpha.sh |
| Power sampler log | ~/jarvis-alpha/logs/power_sampler.log |
| Power ingest | POST /v1/metrics/power (no auth) |
| Power current | GET /v1/metrics/power/current (auth required) |
| Cost summary | GET /v1/costs/summary (auth required) |
| GCP key | ~/jarvis/.gcp_billing.json (Gateway, chmod 600) |
| Migrations applied | 001–011 |
| Handoffs dir | ~/jarvis-alpha/docs/handoffs/ |

## Post-Handoff Fixes (same session)

### Power Sampler — Final State
- Brain switched from powermetrics (CPU pkg only, 0.5W) to psutil estimation
- Brain TDP: idle=20W, max=60W — shows ~21W at light load (realistic)
- All 4 nodes now use consistent psutil method
- psutil installed on Brain venv manually (not in requirements.txt — add next session)
- Final DB readings: Brain 21W, Gateway 9-10W, Endpoint 8W, Sandbox 10-11W ✅

### Open Item Added
- Add psutil to Brain requirements.txt (currently installed manually)
