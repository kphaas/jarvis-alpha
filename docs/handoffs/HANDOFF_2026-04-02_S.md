# HANDOFF_2026-04-02_S.md
Session S — PIN Auth, CORS, apiFetch, Health/Mesh/Home wiring, Sandbox node, UDM Pro bar, auto scp to Endpoint

Last commit: 1b5aea8
Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100
Alpha DB: jarvis_alpha on Brain Postgres 16

---

## What Shipped This Session

### 1. PIN Auth Route
File: brain/routes/pin_auth.py
- POST /v1/auth/pin — validates ALPHA_PIN, issues 30-day RS256 JWT
- Key path from ALPHA_JWT_PRIVATE_KEY env, fallback ~/jarvis/pki/jwt/jwt_private.pem
- Returns { token, expires_at }

### 2. JWT Middleware — Skip Paths + OPTIONS
File: brain/middleware/jwt_auth.py
- /v1/auth/pin added to SKIP_PATHS
- OPTIONS method bypass added — fixes CORS preflight 401

### 3. CORS Middleware
File: brain/app.py
- CORSMiddleware added — allow_origins: https://jarvis-endpoint.tail40ed36.ts.net:4100
- LIFO order: CORS runs before JWT
- Fixes all preflight 405/401 errors from browser

### 4. Lifespan Hook
File: brain/app.py
- recover_stuck_graphs(db_pool) wired to FastAPI lifespan on startup
- Verified: "Recovered 0 stuck graph(s)" on Brain startup

### 5. Dispatch Real Handlers
File: brain/tasks/dispatch.py
- call_llm_agent: local Ollama llama3.1:8b, 60s timeout
- call_code_agent: local Ollama qwen2.5-coder:7b, system prompt injected
- call_tool_agent: explicit stub, WORKER_UPGRADE_PATH comment intact
- Alpha fully isolated — zero calls to jarvis-core

### 6. PinGate UI Component
File: ui/src/components/PinGate.tsx
- Blocks UI until authenticated
- Checks localStorage alpha_token exp on mount
- PIN modal: full-screen overlay, auto-focus, loading + error states
- On 200: stores token, renders children. On 401: "Invalid PIN"

### 7. App.tsx Wired
File: ui/src/App.tsx
- PinGate wraps entire app return

### 8. apiFetch Utility
File: ui/src/lib/apiFetch.ts
- apiFetch(): always uses VITE_BRAIN_URL + injects Bearer token from localStorage
- apiJson<T>(): throws Error("HTTP Nxx") on non-ok, returns parsed JSON
- Used by all pages — replaces all raw fetch() calls

### 9. VITE_BRAIN_URL Fixed
File: ui/.env.local
- Was: https://jarvis-endpoint.tail40ed36.ts.net:4100 (wrong — Endpoint nginx)
- Now: https://jarvis-brain.tail40ed36.ts.net:8186 (correct — Alpha Brain)

### 10. Health Page Fixed
File: ui/src/pages/Health.tsx
- All fetch() replaced with apiJson()
- Routes corrected: /health (not /v1/health), /v1/home/summary, /v1/mesh/status
- Lucide icons added: Brain=Cpu, Gateway=Globe, Endpoint=Monitor, Sandbox=FlaskConical
- Node cards rendered dynamically from API response (not hardcoded 3)

### 11. Home Page Wired
File: ui/src/pages/Home.tsx
- System Status: GET /health
- Last Overnight: GET /v1/tasks/graphs?limit=1
- Cloud Spend section hidden until /v1/costs/summary is built
- Uptime NaN fixed: shows — if uptime_seconds missing
- Each section fails independently

### 12. Mesh Page Fixed
File: ui/src/pages/Mesh.tsx
- All fetch() replaced with apiJson()
- Removed all /api/v1/state calls
- Topology layout redesigned: Brain center, Gateway/Endpoint top, iPhone left, Unraid/Sandbox bottom
- ViewBox height expanded to 400
- iPhone connects to Gateway (not Brain)
- Internet ellipse repositioned + dashed line fixed
- Lucide icons on topology nodes via foreignObject
- iPhone offline no longer marks mesh DEGRADED
- UDM Pro bar at bottom: NETWORK LAYER · UDM-PRO, WAN ↑/↓, Clients

### 13. Sandbox Node Added
DB: alpha_node_registry on Brain Postgres
- Sandbox inserted: 100.124.172.14, node_type=service, health_endpoint=http://100.124.172.14:5001/health
- Appears on Health page node cards and Mesh topology

### 14. UniFi Proxy — Alpha Isolated
Files: gateway/routes/unifi.py, brain/routes/unifi.py
- Gateway talks directly to UDM Pro (192.168.1.1) — stub WAN/clients for now
- Brain proxies to alpha Gateway only — zero jarvis-core calls
- Routes: /v1/unifi/status, /v1/unifi/wan, /v1/unifi/clients, /v1/unifi/summary
- UNIFI_USER, UNIFI_PASSWORD, UNIFI_BASE_URL in ~/jarvis/.secrets on Gateway ✅

### 15. Commit Script — Auto SCP to Endpoint
File: scripts/jarvisalpha_commit.sh
- Step 8 added: scp ui/dist to jarvisendpoint@100.87.223.31 after Sandbox pull
- No more manual scp after commits
- Step 9 renamed: Intel refresh

---

## Smoke Tests Passed
- POST /v1/auth/pin → token + expires_at ✅
- Lifespan hook → "Recovered 0 stuck graph(s)" ✅
- Health tab → all 6 nodes loading, icons visible ✅
- Mesh tab → SYSTEM NOMINAL, topology visible, UDM Pro bar ✅
- Home tab → ok status, Last Overnight loading ✅
- Auto scp → ✅ UI dist → Endpoint on every commit ✅

---

## Known Issues / Next Session

| # | Issue | Priority |
|---|---|---|
| 1 | Mesh topology needs minor visual tweaks — Sandbox/iPhone overlap, graphic crop | P1 |
| 2 | UniFi WAN + clients are stubs — wire real UDM Pro API endpoints | P1 |
| 3 | nginx alpha.conf not in git — lives on Endpoint only | P1 |
| 4 | dispatch.py call_tool_agent is stub | P1 |
| 5 | WebSocket for Buddy events — polling only | P1 |
| 6 | /v1/home/summary costs_today_usd always null | P2 |
| 7 | Health page LaunchAgent section not built | P2 |
| 8 | Home Service Map Gateway/Endpoint/Sandbox show "pending" | P2 |
| 9 | Agent hierarchy contract not formally documented | P1 |
| 10 | Child profile RLS (Ryleigh/Sloane) — KI-13 equivalent | P1 |
| 11 | Executor runs in-process — migrate to LaunchAgent at >5 concurrent graphs | P3 |
| 12 | Worker node architecture — Phase 10+ | P3 |
| 13 | jarvis-core KI-1 through KI-13 still open | Various |
| 14 | Sandbox health endpoint — forge dashboard on :5001 not always running | P2 |

---

## Architecture Decisions This Session
- Alpha fully isolated from jarvis-core — no cross-repo calls anywhere
- UniFi path: Alpha Brain → Alpha Gateway → UDM Pro (HIPAA minimum access, fewer breach vectors)
- UI dist built on Air, auto-scp to Endpoint on every commit (Option B)
- CORS: allow_origins locked to Endpoint Tailscale hostname only

---

## Next Session Priorities (Ordered)
1. Wire real UDM Pro API endpoints in gateway/routes/unifi.py
2. Fix Mesh topology visual tweaks
3. Commit nginx alpha.conf to repo + Endpoint deploy step
4. Wire Home Service Map to real node health data
5. Health page LaunchAgent section

---

## Key Reference

| Item | Value |
|---|---|
| Alpha Brain URL | https://jarvis-brain.tail40ed36.ts.net:8186 |
| Alpha UI URL | https://jarvis-endpoint.tail40ed36.ts.net:4100 |
| Alpha DB | jarvis_alpha on Brain Postgres 16 |
| Brain venv | ~/jarvis-alpha/.venv (python3.12) |
| Commit script | bash ~/jarvis-alpha/scripts/jarvisalpha_commit.sh "msg" |
| Pull script | bash ~/jarvis-alpha/scripts/jarvisalpha_pull.sh |
| Restart alpha | bash ~/jarvis-alpha/scripts/restart_alpha.sh |
| Health check | bash ~/jarvis-alpha/scripts/check_alpha.sh |
| Gen test token | python3 ~/jarvis-alpha/scripts/gen_test_token.py |
| JWT private key | ~/jarvis/pki/jwt/jwt_private.pem |
| Alpha public key | ~/jarvis-alpha/brain/pki/jwt_public.pem |
| Secrets (Brain) | ~/.secrets |
| Secrets (Gateway) | ~/.secrets |
| nginx config (Endpoint) | /opt/homebrew/etc/nginx/servers/alpha.conf |
| apiFetch utility | ~/jarvis-alpha/ui/src/lib/apiFetch.ts |
| PIN auth route | ~/jarvis-alpha/brain/routes/pin_auth.py |
| JWT middleware | ~/jarvis-alpha/brain/middleware/jwt_auth.py |
| Dispatch handlers | ~/jarvis-alpha/brain/tasks/dispatch.py |
| PinGate component | ~/jarvis-alpha/ui/src/components/PinGate.tsx |
| UniFi Brain proxy | ~/jarvis-alpha/brain/routes/unifi.py |
| UniFi Gateway route | ~/jarvis-alpha/gateway/routes/unifi.py |
| Mesh page | ~/jarvis-alpha/ui/src/pages/Mesh.tsx |
| Health page | ~/jarvis-alpha/ui/src/pages/Health.tsx |
