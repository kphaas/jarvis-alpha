# JARVIS Alpha — Session Handoff 2026-04-15 #04

**Session focus:** Child Profile RLS → Service Identity → Token Rotation → Approval Gateway hardening → Settings UI + PIN management → TD-60 cleanup

**Status:** All nodes on `88fa32c`. Brain healthy. Full RLS + Service Identity + Approval Gateway complete.

---

## Commits Shipped

| # | Commit | What |
|---|---|---|
| 1 | `34f3d0d` | Child profile RLS — helper, policies, backfill migrations + rls.py child role mapping |
| 2 | `d2729c5` | TD-60 legacy child RLS policies documented |
| 3 | `6ced589` | Service identity keypairs + gitignore hardening |
| 4 | `50ce0cd` | service_identity.py — brain added, 24hr token lifetime |
| 5 | `3b974a2` | gen_service_token.py restored from history |
| 6 | `585c53e` | Service token rotation — brain_service + endpoint nodes + LaunchAgents |
| 7 | `926eaf0` | PyJWT + cryptography added to requirements.txt |
| 8 | `7b93604` | Approval gateway — decide_approval SECDEF + rls_connection on pending/decide |
| 9 | `ec36c46` | Settings page — family profiles + child PIN management UI |
| 10 | `eb3ed58` | Admin PIN reset UI + set-admin-pin endpoint + set-child-pin T2 classification fix |
| 11 | `88fa32c` | TD-60 — drop 8 legacy PERMISSIVE child policies |

---

## Child Profile RLS — COMPLETE (AI-6 CLOSED)

### What was built
- `rating_allowed()` IMMUTABLE helper function — migration `20260414_170000`
- 5 RESTRICTIVE child policies — migration `20260414_180000`
- `content_rating` backfill to `all_ages` — migration `20260414_190000`
- `brain/db/rls.py` — `child` role maps to `jarvis_role = 'child'`

### Final child policy state (5 RESTRICTIVE only)
| Table | Policy | Logic |
|---|---|---|
| `alpha_conversation_memory` | `child_content_filter` | `rating_allowed(content_rating, app.max_rating)` |
| `chat_threads` | `child_profile_scope` | `owner_profile = jarvis.current_user` |
| `chat_messages` | `child_messages_scope` | thread ownership JOIN |
| `alpha_task_graphs` | `child_profile_scope` | `owner_profile = jarvis.current_user` |
| `alpha_dream_sessions` | `child_profile_scope` | `owner_profile = jarvis.current_user` |

### Profile state
| Profile | Role | max_rating | PIN |
|---|---|---|---|
| Ken | admin | adult | PLACEHOLDER (can set via Settings UI) |
| Ryleigh | child | age_8_plus | ✅ Set |
| Sloane | child | all_ages | ✅ Set this session |

---

## Service Identity — COMPLETE

### Keypairs
| Node | Private Key | Public Key on Brain | LaunchAgent |
|---|---|---|---|
| Brain | `~/jarvis/pki/services/brain_private.pem` | ✅ | `com.jarvis.alpha.rotate.brain_service` |
| Gateway | `~/jarvis/pki/services/gateway_private.pem` | ✅ | `com.jarvis.alpha.rotate.gateway` |
| Endpoint | `~/jarvis/pki/services/endpoint_private.pem` | ✅ | `com.jarvis.alpha.rotate.endpoint` |
| Sandbox | `~/jarvis/pki/services/sandbox_private.pem` | ✅ | `com.jarvis.alpha.rotate.sandbox` |
| Buddy | `~/jarvis/pki/services/buddy_private.pem` | ✅ | `com.jarvis.alpha.rotate.buddy` |

### Public keys in repo
`brain/pki/services/` — all 5 public keys committed. `_build_key_registry()` in `jwt_auth.py` scans this dir automatically.

### Token rotation
- All nodes rotate every 24hr via LaunchAgent
- `TOKEN_LIFETIME_DAYS = 1` in `scripts/service_identity.py`
- Brain node: rotates both buddy token (`ALPHA_BUDDY_TOKEN`) and brain service token (`ALPHA_BRAIN_SERVICE_TOKEN`)
- Endpoint: rotates `ALPHA_SERVICE_TOKEN` — pre-staged for future SSR use

### Generate a service token manually
```bash
cd ~/jarvis-alpha && source .venv/bin/activate
python3 scripts/gen_service_token.py --iss brain
python3 scripts/gen_service_token.py --iss brain --json
```

---

## Approval Gateway — COMPLETE

### What was fixed this session
- `decide_approval()` SECDEF function — migration `20260415_100000`
- `/pending` — moved from bare `pool.acquire()` to `rls_connection`
- `/decide` — bare UPDATE+INSERT replaced with `decide_approval()` SECDEF call
- `set-child-pin` reclassified from `admin` (T5) to `write + security_write` (T2)
- `set-admin-pin` classified as `write + security_write` (T2)
- `security_write` action class added to TIER_RULES → T2

### Full T1-T5 flow status
| Tier | Status |
|---|---|
| T1 — read pass-through | ✅ |
| T2 — write pass-through | ✅ |
| T3 — cost-incurring log | ✅ |
| T4/T5 — queue + block + notify | ✅ |
| `/unlock` PIN → 5min approval token | ✅ |
| `/pending` list under RLS | ✅ |
| `/decide` via SECDEF | ✅ |
| Approvals UI page | ✅ |

---

## Settings UI — COMPLETE

### Route: `/settings`
- Family Profiles section — Ken / Ryleigh / Sloane cards
- Child PIN form — lock icon toggles PIN form per child
- Admin Security section — Change Admin PIN form (current + new + confirm)

### Endpoints wired
- `POST /v1/auth/set-child-pin` — admin scope, sets bcrypt hash for child
- `POST /v1/auth/set-admin-pin` — verifies current PIN, sets new bcrypt hash for Ken

### Ken PIN status
Ken's `pin_hash` is still `PLACEHOLDER_MIGRATE_FROM_ALPHA_PIN` — reads from `ALPHA_PIN` secret.
To migrate to real bcrypt hash: go to `/settings` → Admin Security → enter current PIN from `ALPHA_PIN` secret → set new PIN.

---

## TD-60 — CLOSED

Migration `20260415_110000` dropped all 8 legacy PERMISSIVE child policies.
Only 5 RESTRICTIVE policies remain. Policy set is clean.

---

## RLS Coverage — Final State

All 16 tables under FORCE RLS. No changes this session to coverage — inherited from `58c9368`.

---

## Node State at Session Close

| Node | Commit | Status |
|---|---|---|
| Brain | `88fa32c` | ✅ Healthy, pulled + restarted |
| Gateway | `88fa32c` | ✅ Pulled |
| Endpoint | `88fa32c` | ✅ Pulled |
| Sandbox | `88fa32c` | ✅ Auto-pulled |
| Air | `88fa32c` | ✅ Pulled |

---

## Open Items

| # | Item | Priority |
|---|---|---|
| 1 | **TaskGraph schema + migration** | P1 — Alpha-2 foundation |
| 2 | Service tokens wired into node-to-node calls | P1 — needed for Approval Gateway inter-node |
| 3 | Ken PIN migration (PLACEHOLDER → bcrypt) | P2 — do via Settings UI |
| 4 | TD-58 — smoke_lib.sh v2 | P3 |
| 5 | Sandbox git committer identity | P3 |

---

## Tech Debt Open

| TD | Description | Priority |
|---|---|---|
| TD-58 | smoke_lib.sh v2 — load_secrets + migrate remaining 4 smokes | P3 |

---

## Next Session Plan

### Primary: TaskGraph — Alpha-2 Foundation

1. Design review — TaskGraph schema (DAG structure, step states, checkpoint/resume)
2. Migration — `alpha_task_graphs` + `alpha_task_steps` + `alpha_task_events` full schema
3. SECDEF wrappers for TaskGraph writes
4. FastAPI routes — submit / status / cancel / approve step
5. Executor stub — in-process, LaunchAgent upgrade path documented

### Secondary (if time)
- Wire service tokens into Gateway → Brain calls
- Ken PIN migration

---

## Quick Reference

| Item | Path |
|---|---|
| Child RLS helper | `brain/db/migrations/20260414_170000_child_rls_helper.sql` |
| Child RLS policies | `brain/db/migrations/20260414_180000_child_rls_policies.sql` |
| decide_approval SECDEF | `brain/db/migrations/20260415_100000_decide_approval_secdef.sql` |
| TD-60 cleanup | `brain/db/migrations/20260415_110000_td60_drop_legacy_child_policies.sql` |
| Service identity constants | `scripts/service_identity.py` |
| Token generator | `scripts/gen_service_token.py` |
| Token rotator | `scripts/rotate_service_token.py` |
| Approval middleware | `brain/middleware/approval.py` |
| Approval routes | `brain/routes/approvals.py` |
| Settings UI | `ui/src/pages/Settings.tsx` |
| PIN auth routes | `brain/routes/pin_auth.py` |
| Public keys repo | `brain/pki/services/` |
| Previous handoff | `docs/handoffs/HANDOFF_2026-04-14_03.md` |

---

*TaskGraph next. Every table is under FORCE RLS. Service identity live on all nodes. Approval Gateway T1-T5 complete. Child profiles DB-enforced.*
