# HANDOFF_2026-04-04_02.md
Session — Security Operations Center + jarvis-common + Dream Mode Spec

Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100

---

## What Shipped This Session (Part 2 of Apr 4)

### 1. Gateway Claude Adapter Fix — diagnose.py
- Payload mismatch fixed: `adapter` → `provider`, messages nested in `payload` with `model` + `system`
- Response parsing fixed: extracts `result.content[].text` from Anthropic response
- End-to-end verified: Brain → Gateway → Anthropic → parsed diagnosis
- Gateway was alive on :8283 the whole time — port confusion with :8282 was stale manual curl
- Unblocked: AI Diagnose (Claude mode), Ask cloud escalation, Dream Mode planning

### 2. jarvis-common Shared Package
- Created `common/jarvis_common/` with `logging_config.py` + `secrets.py`
- `pyproject.toml` ready for wheel builds
- 25 imports updated across Brain (18 logging + 4 secrets) and Gateway (7 logging + 3 secrets)
- 3 re-export shims kept for backward compatibility (brain/config/logging_config.py, brain/config/secrets.py, gateway/config/logging_config.py)
- PYTHONPATH extended in start scripts and LaunchAgent plists
- Cursor handled PYTHONPATH automatically during the edit
- Forge/Sandbox wheel integration deferred — separate step

### 3. Dream Mode Spec V1 — Committed
- File: ~/jarvis-alpha/docs/DREAM_MODE_SPEC.md
- 20 sections, 500+ lines
- 16 gaps identified and addressed (7 HIGH, 4 MEDIUM, 1 RESOLVED)
- Perplexity review incorporated: atomic session state, caffeinate, cost cap, drain window
- Self-review added: dry-run mode, kill switch, pre-flight probe, resource locking, canary, graph size limit, post-action verification, child profile enforcement
- Dendrite Matrix on Unraid for notifications (not Synapse — too heavy)
- Build order: 15 steps, ~15 hours across 3-4 sessions
- Open questions: Dendrite backend, max session duration, schedule, cost budget default

### 4. Mesh Topology Fixes + Enhancements
- Overlap fix: bottom row spread to y:380, iPhone x:100, Unraid x:280, Sandbox x:640
- Animated data flow dots: SVG animateMotion along path elements, staggered durations
- UDM Pro router icon: Lucide Router in foreignObject, status dot
- Live request ticker: RequestTicker component, polls /v1/logs/query, 15s refresh

### 5. Security API — 4 Brain Endpoints
- GET /v1/security/jwt-check — probes all routes without token, confirms 401/200
- GET /v1/security/rls-status — queries pg_policies catalog (polname→policyname fix)
- GET /v1/security/child-profiles — enforcement status per child
- GET /v1/security/perimeter — CORS, port scan, Tailscale status
- File: brain/routes/security.py (360 lines)

### 6. Security Dashboard UI — Full Rewrite
- 8 sub-tabs: Overview, Identity, Network, Certs, Keys, Honeypot, MCP, Events
- Security Posture Score: SVG donut, 0-100 weighted calculation, color-coded
- JWT enforcement table: 10/10 routes verified live
- RLS status: 8/33 tables protected, split Protected/Unprotected view
- CORS + port scan + Tailscale status
- Cert days remaining with progress bars
- Auth events from Loki
- File: ui/src/pages/Security.tsx (1875 lines)

### 7. Security Probe Fixes (4 issues)
- Tailscale: full path /opt/homebrew/bin/tailscale (now shows 9 nodes active)
- Loki 3100: changed from HTTPS to http://localhost:3100/ready (now reachable)
- Ollama 11434: changed from HTTPS to http://localhost:11434/api/tags (now reachable)
- Power sampler: added /v1/metrics/power + /current + /history + /rollup to JWT SKIP_PATHS

### 8. Secrets Audit Trail
- Callback hook pattern: jarvis_common.secrets._audit_hooks + register_audit_hook()
- clear_cache() added for key rotation support
- brain/audit/secret_audit.py: Postgres writer with buffer, async flush
- Table: secret_access_log (key_name, source, accessed_at, node)
- API: GET /v1/security/secrets-audit
- Hook registered in Brain lifespan, cache cleared after registration
- ISO string → datetime fix for asyncpg executemany

### 9. API Key Rotation System
- Gateway: gateway/routes/admin.py (299 lines)
  - POST /v1/admin/rotate-key with Bearer GATEWAY_TOKEN auth
  - Atomic file write: tempfile.mkstemp() + os.replace()
  - Backup before write: .bak file, restored on failure
  - 5-second drain window after cache clear
  - Key format validation: prefix + min_length + ASCII check
  - Provider test: real API call with new key
  - Rollback on test failure: restore file + env + cache
  - asyncio.Lock() prevents concurrent rotations
- Brain: brain/routes/rotation.py (165 lines)
  - POST /v1/security/rotate-key — proxy to Gateway
  - GET /v1/security/rotatable-keys — format rules for UI
  - Logs rotation events to secret_access_log
  - asyncio.Lock() on Brain side too
- UI: Keys sub-tab with rotation modal
  - Paste field for new key from vendor dashboard
  - Client-side format validation (prefix + length)
  - Loading state, success/rollback/error result display
  - Secrets audit table below key list

### 10. Honeypot Decoy Endpoints
- brain/routes/honeypot.py (161 lines)
- 6 traps: /admin, /wp-login.php, /.env, /.git/config, /phpmyadmin, /api/v1/debug
- Fake content clearly marked FAKE/HONEYPOT
- In-memory event store (cap 500)
- GET /v1/honeypot/events (JWT protected)
- Decoy paths added to JWT SKIP_PATHS
- Logs as WARNING level — visible in Events tab
- UI: Honeypot sub-tab with armed trap cards + hits table

### 11. MCP Server Registry
- brain/routes/mcp_registry.py (59 lines)
- GET /v1/security/mcp/registry
- 4 planned servers: Home Assistant, UniFi, Google Calendar, Unraid
- Tracks: permissions, endpoint, backlog reference, status
- UI: MCP sub-tab with server cards and permission badges

---

## Current LaunchAgent Inventory

### Brain (7 agents)
| Agent | Status |
|---|---|
| com.jarvis.alpha.brain | RUNNING |
| com.jarvis.alpha.executor | RUNNING |
| com.jarvis.alpha.watchdog | RUNNING |
| com.jarvis.alpha.buddy | RUNNING |
| com.jarvis.alpha.power.brain | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |
| com.jarvis.alpha.loki | RUNNING |

### Gateway (3 agents)
| Agent | Status |
|---|---|
| com.jarvis.alpha.gateway | RUNNING |
| com.jarvis.alpha.power.gateway | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |

### Endpoint (3 agents)
| Agent | Status |
|---|---|
| com.jarvis.nginx | RUNNING |
| com.jarvis.alpha.power.endpoint | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |

### Shared
| Agent | Node | Status |
|---|---|---|
| com.jarvis.certrenew | Brain, Gateway, Endpoint | KEPT |
| com.jarvis.ollama | Brain | KEPT |

---

## Files Changed This Session

### New Files
- common/jarvis_common/__init__.py
- common/jarvis_common/logging_config.py
- common/jarvis_common/secrets.py
- common/pyproject.toml
- brain/audit/__init__.py
- brain/audit/secret_audit.py
- brain/routes/security.py
- brain/routes/rotation.py
- brain/routes/honeypot.py
- brain/routes/mcp_registry.py
- gateway/routes/admin.py
- docs/DREAM_MODE_SPEC.md

### Modified Files
- brain/app.py (5 new router mounts + audit hook registration)
- brain/middleware/jwt_auth.py (SKIP_PATHS: metrics + honeypot)
- brain/config/secrets.py (re-export shim)
- brain/config/logging_config.py (re-export shim)
- brain/routes/diagnose.py (Claude payload fix)
- gateway/app.py (admin_router mount)
- gateway/config/logging_config.py (re-export shim)
- ui/src/pages/Mesh.tsx (overlap fix + enhancements, 691 lines)
- ui/src/pages/Security.tsx (full rewrite, 1875 lines)
- 18 Brain files (import updates to jarvis_common)
- 10 Gateway files (import updates to jarvis_common)

---

## GitHub Issues Status

### Can Close
- #10 Mesh topology visual overlap — FIXED (coordinate spread)

### Remaining Open
- #9 UniFi WAN + clients stubs (Gateway side)
- #5 Forge per-project isolation (forge scope)
- #12 Home Service Map shows "pending"
- #13 Health page LaunchAgent section
- #14 costs_today_usd always null

### New Issues to Create
- Cert management pipeline (generate, validate, distribute, reload nginx)
- Wire real UDM Pro API (firewall rules, clients, threat management)
- Agent Security sub-tab (Dream Mode permissions, JWT scopes)
- jarvis-common wheel build + Forge/Sandbox pip install
- Ruff lint rule to flag raw `import logging`
- Shared jarvis-common for Forge (currently still uses api.config.logging_config)
- GATEWAY_TOKEN duplicate entries in ~/jarvis/.secrets (Brain + Gateway both have 2)
- Honeypot: persist events to Postgres (currently in-memory only)
- MCP Registry: wire real status checks when servers are built
- Dream Mode build (15 steps from spec)
- Dendrite Matrix install on Unraid
- Clean up .claude/worktrees on Air

---

## Known Issues

- Sandbox :5001 shows unreachable in port scan — forge was offline during testing
- GATEWAY_TOKEN appears twice in ~/jarvis/.secrets on both Brain and Gateway (209 chars total)
- Honeypot events are in-memory only — lost on Brain restart
- MCP servers all show "planned" — no real integrations yet
- Secrets audit only captures Brain-side accesses — Gateway has no audit hook registered
- Posture score maxes at 90/100 (10 pts reserved for future: secrets rotation age, remaining honeypot)

---

## Next Session Priorities

1. Dream Mode — start building (schema migration, Brain routes, Gateway orchestrator)
2. Cert management pipeline
3. Wire real UDM Pro API
4. jarvis-common wheel build for Forge
5. Honeypot events to Postgres (survive restarts)

---

## Quick Reference

| Item | Value |
|---|---|
| jarvis-common package | ~/jarvis-alpha/common/jarvis_common/ |
| Security API | brain/routes/security.py (360 lines) |
| Security UI | ui/src/pages/Security.tsx (1875 lines) |
| Rotation API (Brain) | brain/routes/rotation.py (165 lines) |
| Rotation API (Gateway) | gateway/routes/admin.py (299 lines) |
| Honeypot | brain/routes/honeypot.py (161 lines) |
| MCP Registry | brain/routes/mcp_registry.py (59 lines) |
| Secrets audit | brain/audit/secret_audit.py (94 lines) |
| Dream Mode spec | docs/DREAM_MODE_SPEC.md |
| Mesh page | ui/src/pages/Mesh.tsx (691 lines) |
| Gateway port | 8283 (NOT 8282) |
| Gateway admin auth | Bearer GATEWAY_TOKEN |
| Rotatable keys | ANTHROPIC_API_KEY, GEMINI_API_KEY, PERPLEXITY_API_KEY |
| Honeypot traps | /admin, /wp-login.php, /.env, /.git/config, /phpmyadmin, /api/v1/debug |
