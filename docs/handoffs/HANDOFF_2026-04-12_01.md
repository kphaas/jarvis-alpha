# JARVIS Alpha — Session Handoff 2026-04-12 #01

**Session focus:** Multi-day session spanning 2026-04-10 through 2026-04-12. Morning triage on 04-10 → TD-46 closed (self-healed) → TD-49 closed (code+smoke shipped) → Stage 5d discovery → narrowed scope decision (Temporal POC parallel path) → Stage 5d.1 watchdog_agent cutover shipped → 42+ hour soak → smoke script hardened to final form on 04-12.

**Status:** Stage 5d.1 LIVE and soaked 42+ hours. PID 3518 stable since 2026-04-10 21:29 UTC. Zero errors. Ready for Temporal POC + Stage 5d.2.

---

## Commits Shipped

| # | Commit | What |
|---|---|---|
| 1 | (td-49) | fix(td-49): rename priority→severity + map high→warning + structured error logs + smoke |
| 2 | (smoke-cte) | fix(smoke): wrap task_events INSERT in CTE + add -X to all psql calls |
| 3 | (docs) | docs(stage-5d): phase 2 design + temporal POC plan |
| 4 | (5d1) | feat(stage-5d1): watchdog_agent cutover to jarvis_alpha_writer + smoke |
| 5 | (smoke-cols) | fix(smoke-5d1): correct column names + valid event_type |
| 6 | 6c3e0e7 | fix(smoke-5d1): filter psql multi-statement output through UUID regex |
| 7 | b868f4e | fix(smoke-5d1): use admin for DELETE + row-count assertion |

---

## TD-46 — Buddy Priority Type Mismatch (CLOSED — self-healed)

Migration 20260408_130000 created record_buddy_event SECDEF with p_priority TEXT against INTEGER column. Migration 20260408_140000 (31 min later) dropped and recreated with correct type. 64 error entries from 04-08 20:08-20:39 UTC window. Buddy clean since. Documentation-only closure.

## TD-49 — alpha_task_events priority→severity Drift (CLOSED)

Live schema had renamed `priority` → `severity` and changed CHECK values. watchdog.py:111 and executor.py:497 still used old column + old value 'high'. Dormant (try/except swallowed errors). Fixed both files, added structured JSON error logging, created smoke_task_events_insert.sh with positive + negative tests.

## Stage 5d Discovery (Sandbox Claude)

971-line STAGE5D_DISCOVERY.md. Two critical findings:
- **R1 (BLOCKER for 5d.2):** executor.py/watchdog.py set jarvis.role='admin', policies check 'platform_admin'. Silent failure waiting.
- **R2 (DORMANT):** TaskGraphExecutor uses 5 invalid status values violating CHECK constraints. Would fire on first graph execution.

Task tables verified empty (0 rows). No production impact from R2 today.

## Strategic Decision — Temporal POC Parallel Path

Identified Temporal would eliminate R1/R2/TD-44 bug class entirely. Decision: narrow Stage 5d to R1 fix + cutover (defer R2), run 2-4hr Temporal POC, decide on Alpha-2 rewrite. STAGE5D_DESIGN.md + TEMPORAL_POC_PLAN.md committed.

## Stage 5d.1 Cutover (SHIPPED + SOAKED 42h)

**Code:** brain/agents/watchdog_agent.py:358 — os.environ["ALPHA_DB_DSN"] → get_secret("ALPHA_DB_DSN_WATCHDOG_AGENT").
**Secrets:** ALPHA_DB_DSN_WATCHDOG_AGENT added (peer auth, same DSN as WRITER).
**Smoke:** 5 tests — connect/SELECT/INSERT/DELETE-via-admin/negative-RLS. All pass.
**Production:** PID 3518 stable 2026-04-10 21:29 → 2026-04-12 15:35+, ~42h clean cycles, zero errors.

## Smoke Script Hardening (3 iterations)

Three psql surprises caught in one session:
1. Command tag leak (INSERT 0 1) — fixed with CTE wrap
2. Multi-statement output concatenation (BEGINsystem<uuid>COMMIT) — fixed with UUID regex grep
3. Silent DELETE under writer (no DELETE policy by design) — fixed with admin connection + row-count assertion

**Architectural win surfaced:** alpha_watchdog_events is append-only under writer role. Compromised writer cannot tamper with audit history. Delivered more security than designed.

---

## Tech Debt Logged

| TD | Description | Priority |
|---|---|---|
| TD-50 | alpha_task_events forward migration path undocumented | P3 |
| TD-51 | ruff E402 in scripts/rotate_service_token.py:44 | P3 |
| TD-52 | Overnight Sandbox Claude runs need completion verification | P2 |
| TD-53 | TaskGraphExecutor invalid status values — fix OR delete pending Temporal | P0-when-decided |
| TD-54 | Delete tasks/watchdog.py (dead code) — scheduled 5d.2 | scheduled |
| TD-55 | Task tables empty — TaskGraph build-out unstarted | INFO |
| TD-56 | Silent-failure monitoring for executor | P2 |
| TD-57 | Build scripts/smoke_lib.sh with psql_capture_uuid + psql_admin + assert_row_count | P2 |

## Tech Debt Closed
| TD | How |
|---|---|
| TD-46 | Self-healed via migration 140000 — documented |
| TD-49 | Code + smoke shipped |

---

## Lessons Locked

1. Sandbox Claude Phase 1 discovery delivers real value — found R1 + R2 that would have shipped broken.
2. Smoke script surprises come in threes. Invest in smoke_lib.sh before next smoke.
3. Empty production tables let us fix R2 in place OR delete whole class via Temporal.
4. Big-tech long-term view prevented wasted work on R2.
5. Production cutover and smoke script are separate concerns — live log evidence proved cutover before smoke was fixed.
6. Audit log tables SHOULD reject DELETE under writer. Silent DELETE was a security feature.
7. 24h soak minimum is the floor. 42h soak across multi-day gap = even better signal.

---

## Next Session Plan

1. **Temporal POC (2-4 hours)** per TEMPORAL_POC_PLAN.md. Write decision to TEMPORAL_DECISION.md.
2. **If Temporal = GO:** Stage 5d.2 ships only R1 GUC fix + DSN cutover (skip R2, executor dies in Alpha-2).
3. **If Temporal = NO-GO:** Stage 5d.2 full scope — R1 + R2 + delete tasks/watchdog.py + DSN cutover.
4. 24h soak between 5d.1 and 5d.2 already exceeded — proceed when ready.
5. Draft scripts/smoke_lib.sh (TD-57) during 5d.2 soak window.

---

## Working Memory For Next Session

- Latest commit: b868f4e
- Stage 5d.1 LIVE, soaked 42+ hours
- PID 3518 stable, clean 60s cycles
- ALPHA_DB_DSN_WATCHDOG_AGENT in ~/jarvis/.secrets
- Backups: .bak_pre_5d1 + .bak_pre_5d1_ship (identical, pre-ship state)
- STAGE5D_DESIGN.md + TEMPORAL_POC_PLAN.md locked
- Task tables still empty (verified 04-10)
- All 4 nodes healthy

---

## Quick Reference

| Item | Path |
|---|---|
| Stage 5d design | ~/jarvis-alpha/docs/STAGE5D_DESIGN.md |
| Temporal POC plan | ~/jarvis-alpha/docs/TEMPORAL_POC_PLAN.md |
| Stage 5d discovery | ~/jarvis-alpha/docs/STAGE5D_DISCOVERY.md |
| Sandbox Claude runbook | ~/jarvis-alpha/docs/SANDBOX_CLAUDE_RUNBOOK.md |
| Smoke 5d.1 | bash ~/jarvis-alpha/scripts/smoke_5d1_watchdog_agent.sh |
| Smoke task_events | bash ~/jarvis-alpha/scripts/smoke_task_events_insert.sh |

---

*Stage 5d.1 shipped, 42h soaked. Temporal POC next. 5d.2 after decision.*
