# HANDOFF_2026-04-04_01.md
Session — Observability Stack + jarvis-core Decommission

Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100

---

## What Shipped This Session

### 1. jarvis-core Decommissioned — All 3 Nodes
- Brain: 13 LaunchAgents stopped, disabled, booted out, plists archived to ~/jarvis-core-archive/launchagents/
- Gateway: 10 LaunchAgents stopped, disabled, booted out, plists archived
- Endpoint: 7 LaunchAgents stopped, disabled, booted out, plists archived
- nginx jarvis.conf renamed to jarvis.conf.disabled — alpha.conf untouched
- KEPT on all nodes: com.jarvis.certrenew (shared TLS certs), com.jarvis.ollama (Brain only)
- Code stays on disk, GitHub repo untouched — reversible if needed
- Stale nginx master on Endpoint killed (was holding :4000 from Wednesday)

### 2. Test Graph Cleanup
- 7 test graphs deleted from alpha_task_graphs (+ 8 steps, 0 events)
- jarvis_alpha DB now at 0 graphs — clean for Dream Mode

### 3. Grafana Loki — Log Aggregation
- Installed via Homebrew on Brain (v3.7.1)
- Monolithic mode (target: all) with filesystem storage
- Config: ~/jarvis/loki/loki-config.yaml
- Data: ~/jarvis/loki/chunks/, index/, cache/, compactor/
- 30-day retention with compactor
- LaunchAgent: com.jarvis.alpha.loki
- Logs: ~/jarvis/loki/loki-stdout.log, loki-stderr.log
- Key fixes during setup:
  - instance_addr must be at common level (not inside ring) — known Loki bug since 2.5
  - macOS firewall required explicit allow for /opt/homebrew/bin/loki
  - http_listen_address: 0.0.0.0 required for Tailscale IPv4 access

### 4. Fluent Bit — Log Shipping (All 4 Nodes)
- Installed via Homebrew (v5.0.2) on Brain, Gateway, Endpoint, Sandbox
- Config: ~/fluent-bit/fluent-bit.yaml per node
- Position DB: ~/fluent-bit/db/ per node
- LaunchAgent: com.jarvis.alpha.fluentbit per node
- Brain ships to localhost:3100, remote nodes ship to jarvis-brain.tail40ed36.ts.net:3100
- Tails all log files in ~/jarvis-alpha/logs/ (Brain/Gateway), nginx logs (Endpoint), forge logs (Sandbox)

### 5. Structured Logging Module — brain/config/logging_config.py (60 lines)
- JarvisFormatter: JSON output {"ts", "level", "service", "node", "trace_id", "message"}
- contextvars.ContextVar for trace_id propagation across async calls
- get_logger(service) — stdout only (LaunchAgent captures to file, Fluent Bit tails file)
- new_trace_id() — uuid4 hex[:12], set per request/cycle
- set_trace_id() — manual override for batch jobs

### 6. TraceId Middleware — brain/middleware/trace_id.py (17 lines)
- Generates trace_id per incoming HTTP request
- Sets contextvars so all downstream logs share the trace_id
- Adds X-Trace-Id response header (nginx passes through to access logs)
- Added last in app.py middleware stack (runs first in LIFO)

### 7. All Brain Services Wired (16 files modified)
- Zero `import logging` remaining outside logging_config.py
- Services: alpha_brain, alpha_executor, alpha_watchdog, alpha_buddy, alpha_dispatch, alpha_memory
- Executor and watchdog call new_trace_id() at start of each cycle/graph
- Buddy calls new_trace_id() at start of each 60s cycle

### 8. Gateway Structured Logging
- gateway/config/logging_config.py — copy of brain version
- All gateway files wired: app.py, cloud_routes.py, unifi.py, all adapters
- JARVIS_NODE=gateway set in LaunchAgent plist

### 9. Uvicorn Structured Access Logs
- brain/config/uvicorn_log_config.py — UVICORN_LOG_CONFIG dict
- Routes uvicorn.access through JarvisFormatter (service: alpha_brain_access)
- start_alpha_brain.sh updated to use uvicorn.run() with log_config

### 10. nginx JSON Access Logs
- log_format json_structured in nginx.conf http block
- alpha.conf uses json_structured format
- Outputs: {"ts", "level", "service", "node", "trace_id", "message", "client", "upstream_status", "bytes_sent"}
- trace_id passes through from Brain's X-Trace-Id response header
- nginx.conf cleaned up — removed all commented cruft and unused default :8080 server

### 11. jarvis-forge Structured Logging
- api/config/logging_config.py — same module, default node "sandbox"
- 8 files wired: routes, pipeline (planner, reviewer, risk_scorer, learning_agent, failure_recovery), memory (lesson_retriever, embeddings)
- Service names: forge-api, forge-planner, forge-runner, forge-reviewer, forge-memory

### 12. Errors & Logs Page — Datadog-Style Viewer
File: ui/src/pages/Errors.tsx (983 lines)
- Time range selector (15m, 1h, 6h, 24h, 7d)
- Node multi-select (brain, gateway, endpoint, sandbox)
- Level multi-select (INFO, WARNING, ERROR, CRITICAL)
- Service dropdown
- Text search
- Auto-refresh toggle (30s)
- Active filters description bar
- Log table with level badges, clickable trace_ids, expandable messages
- Server-side filtering: UI sends params, Brain builds LogQL and filters parsed results

### 13. Brain Logs Proxy — brain/routes/logs.py (168 lines)
- GET /v1/logs/query — proxies to Loki localhost:3100
- Parses Fluent Bit JSON wrapping, extracts inner structured fields
- Server-side filtering by level, service, text search
- Builds LogQL with node selector + | json | line_format + level/service filters

### 14. AI Diagnose — brain/routes/diagnose.py (141 lines)
- POST /v1/logs/diagnose — diagnoses individual log errors
- Two providers: "local" (Ollama llama3.1:8b) and "claude" (Gateway /v1/cloud/call)
- JARVIS infrastructure context in system prompt
- Context gathering: same trace_id entries or neighboring entries
- UI: Local and Claude buttons on ERROR, CRITICAL, UNKNOWN rows
- Diagnosis panel with code block formatting

### 15. Pattern Analyzer — brain/routes/patterns.py (259 lines)
- GET /v1/logs/analyze-patterns — groups similar logs, LLM diagnoses patterns
- Normalizes messages (strip timestamps, IPs, PIDs) for grouping
- Top 10 patterns by count sent to Ollama
- Returns severity (CRITICAL/HIGH/MEDIUM/LOW), root cause, fix commands
- UI: "Analyze Patterns" button with Sparkles icon
- Analysis panel with severity-colored cards, node badges, fix code blocks

---

## Current LaunchAgent Inventory

### Brain (7 agents)
| Agent | Status |
|---|---|
| com.jarvis.alpha.brain | RUNNING |
| com.jarvis.alpha.executor | RUNNING |
| com.jarvis.alpha.watchdog | RUNNING |
| com.jarvis.alpha.buddy | RUNNING |
| com.jarvis.alpha.power.brain | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |
| com.jarvis.alpha.loki | RUNNING |

### Gateway (3 agents)
| Agent | Status |
|---|---|
| com.jarvis.alpha.gateway | RUNNING |
| com.jarvis.alpha.power.gateway | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |

### Endpoint (3 agents)
| Agent | Status |
|---|---|
| com.jarvis.nginx | RUNNING |
| com.jarvis.alpha.power.endpoint | RUNNING |
| com.jarvis.alpha.fluentbit | RUNNING |

### Sandbox (Fluent Bit + forge)
| Agent | Status |
|---|---|
| com.jarvis.alpha.fluentbit | RUNNING |

### Shared (kept from jarvis-core)
| Agent | Node | Status |
|---|---|---|
| com.jarvis.certrenew | Brain, Gateway, Endpoint | KEPT |
| com.jarvis.ollama | Brain | KEPT |

---

## Logging Coverage

| Service | Node | Logger | Status |
|---|---|---|---|
| Alpha Brain + all routes/middleware | Brain | alpha_brain | ✅ |
| Executor | Brain | alpha_executor | ✅ |
| Watchdog | Brain | alpha_watchdog | ✅ |
| Buddy | Brain | alpha_buddy | ✅ |
| Dispatch | Brain | alpha_dispatch | ✅ |
| Memory | Brain | alpha_memory | ✅ |
| Uvicorn access | Brain | alpha_brain_access | ✅ |
| Alpha Gateway + adapters | Gateway | alpha_gateway | ✅ |
| nginx | Endpoint | nginx_access | ✅ |
| Forge API + pipeline + memory | Sandbox | forge-* | ✅ |
| Ollama | Brain/Sandbox | uncontrollable | ⬜ raw text |

---

## GitHub Issues Status

### Closed This Session
- nginx bind conflict on Endpoint — killed stale master process

### Remaining
- #9 UniFi WAN + clients stubs (alpha side)
- #5 Forge per-project isolation (forge scope)

---

## Next Session Priorities

1. Dream Mode — overnight agent on Gateway using TaskGraph
2. Morning briefing endpoint
3. Voice UI port from jarvis-core (STT/TTS)
4. WebSocket for Buddy events (replace 10s polling)
5. Wire Gateway cloud adapters (Claude endpoint returns "No response" currently)
6. Shared jarvis-common Python package (avoid copy/paste of logging_config)
7. Ruff lint rule to flag raw `import logging`
8. Clean up .claude/worktrees on Air

---

## Known Issues

- Loki labels endpoint slow (~30s response) — query_range works fine
- Ollama logs are raw text, show as UNKNOWN level — can't control Ollama's format
- Claude diagnose returns "No response" — Gateway /v1/cloud/call stub not wired
- .claude/worktrees/loving-hofstadter on Air — leftover from Claude Code session

---

## Quick Reference

| Item | Value |
|---|---|
| Loki config | ~/jarvis/loki/loki-config.yaml |
| Loki data | ~/jarvis/loki/chunks/ |
| Loki port | 3100 (Brain only, HTTP) |
| Fluent Bit config | ~/fluent-bit/fluent-bit.yaml (per node) |
| Fluent Bit position DB | ~/fluent-bit/db/ (per node) |
| Structured logging module | brain/config/logging_config.py |
| Gateway logging module | gateway/config/logging_config.py |
| Forge logging module | api/config/logging_config.py |
| TraceId middleware | brain/middleware/trace_id.py |
| Uvicorn log config | brain/config/uvicorn_log_config.py |
| nginx config | /opt/homebrew/etc/nginx/nginx.conf (Endpoint) |
| nginx alpha config | /opt/homebrew/etc/nginx/servers/alpha.conf (Endpoint) |
| Logs proxy | brain/routes/logs.py |
| Diagnose endpoint | brain/routes/diagnose.py |
| Patterns endpoint | brain/routes/patterns.py |
| Errors page | ui/src/pages/Errors.tsx |
| Core archive (Brain) | ~/jarvis-core-archive/launchagents/ |
| Core archive (Gateway) | ~/jarvis-core-archive/launchagents/ |
| Core archive (Endpoint) | ~/jarvis-core-archive/launchagents/ |
