# JARVIS Alpha — Session Handoff 2026-04-14 #03

**Session focus:** Stage 5d.2 resolution → Stage 7 → Stage 6 (6a through 6d) → semantic_isolation fix → migration runner quiet mode.

**Status:** Full RLS hardening complete. All tables under FORCE RLS. Brain at `58c9368`, healthy. Gateway/Endpoint pull pending.

---

## Commits Shipped

| # | Commit | What |
|---|---|---|
| 1 | `16d39fc` | TD-59 — pull script explicit error on git pull/clone failure |
| 2 | `3f605ff` | Stage 7 — SECDEF wrappers: expire_pending_approvals + record_watchdog_event |
| 3 | `541070f` | Stage 6a — GUC Python changes: rls.py, watchdog.py, dev.py |
| 4 | `d44c6d0` | Stage 6a fix — correct policy names in GUC canonicalize migration |
| 5 | `44259e4` | Stage 6b — SECDEF wrappers + rls_connection: buddy, approval middleware, approvals route |
| 6 | `18fe037` | Stage 6c+6d — FORCE RLS on all 11 remaining tables |
| 7 | `116f78e` | Fix — jarvis.is_admin orphan → canonical jarvis.role in semantic_isolation |
| 8 | `58c9368` | Chore — suppress skipped migration lines (VERBOSE=1 to show) |

---

## Stage 5d.2 — CLOSED (was already done)

Pre-flight discovery confirmed both R1 and DSN cutover were completed in a prior session:
- `executor.py` line 32: `DB_DSN_KEY = "ALPHA_DB_DSN_EXECUTOR"` ✅
- `_bind_executor_rls` / `_bind_worker_rls`: `'platform_admin'` ✅
- `watchdog_agent.py`: `ALPHA_DB_DSN_WATCHDOG_AGENT` ✅

---

## Stage 7 — COMPLETE

SECDEF wrappers for background agent bare DML:

| Function | Table | Migration |
|---|---|---|
| `expire_pending_approvals()` | alpha_approval_queue + alpha_approval_audit | 20260414_110000 |
| `record_watchdog_event(...)` | alpha_watchdog_events | 20260414_120000 |

Python cutover:
- `buddy_agent.py` — `_expire_pending_approvals()` → SECDEF call
- `watchdog_agent.py` — bare INSERT + dead `rls.user_id` GUC → SECDEF call

---

## Stage 6 — COMPLETE

### 6a — GUC Canonicalization
- Legacy GUC families eliminated: `app.profile_role`, `app.profile_id`, `rls.user_id`
- Canonical standard enforced: `jarvis.current_user` + `jarvis.role` (`platform_admin` | `user`)
- Migration: `20260414_130000_guc_canonicalize.sql` — 16 DROP/CREATE POLICY pairs
- Python: `rls.py`, `watchdog.py`, `dev.py` updated

### 6b — SECDEF + rls_connection hardening
- `buddy.py` — all 3 handlers (get, mark_read, mark_all_read) on `rls_connection`
- `middleware/approval.py` — bare DML → `enqueue_approval_request()` + `consume_approved_queue_item()` SECDEF
- `approvals.py` `/decide` handler → `rls_connection`
- `_write_audit` dead code deleted (47 lines)
- Migration: `20260414_140000_approval_rls.sql` — ENABLE RLS + policies + 2 SECDEF functions

### 6c+6d — FORCE RLS
- Migration: `20260414_150000_force_rls_stage6cd.sql`
- alpha_buddy_events: ENABLE + policy + FORCE
- alpha_approval_queue + alpha_approval_audit: FORCE
- 8 remaining tables FORCEd: chat_threads, chat_messages, alpha_task_graphs, alpha_task_steps, alpha_task_events, alpha_dream_sessions, alpha_dream_steps, alpha_watchdog_events

### Semantic isolation fix
- `jarvis.is_admin` orphan replaced with `jarvis.role = 'platform_admin'`
- Migration: `20260414_160000_fix_semantic_isolation.sql`

---

## RLS Coverage — Final State

| Table | ENABLE | FORCE | Policy GUC |
|---|---|---|---|
| alpha_conversation_memory | ✅ | ✅ | jarvis.current_user / jarvis.role |
| alpha_semantic_memory | ✅ | ✅ | jarvis.current_user / jarvis.role ← fixed |
| vault_documents | ✅ | ✅ | jarvis.role |
| vault_pipeline | ✅ | ✅ | jarvis.role |
| vault_access_log | ✅ | ✅ | jarvis.role |
| alpha_buddy_events | ✅ | ✅ | jarvis.role |
| alpha_approval_queue | ✅ | ✅ | jarvis.role |
| alpha_approval_audit | ✅ | ✅ | jarvis.role |
| chat_threads | ✅ | ✅ | jarvis.current_user / jarvis.role |
| chat_messages | ✅ | ✅ | jarvis.current_user / jarvis.role |
| alpha_task_graphs | ✅ | ✅ | jarvis.current_user / jarvis.role |
| alpha_task_steps | ✅ | ✅ | jarvis.current_user / jarvis.role |
| alpha_task_events | ✅ | ✅ | jarvis.role / jarvis.current_user |
| alpha_dream_sessions | ✅ | ✅ | jarvis.role |
| alpha_dream_steps | ✅ | ✅ | jarvis.role |
| alpha_watchdog_events | ✅ | ✅ | SECDEF only (WITH CHECK true) |

---

## Node State at Session Close

| Node | Commit | Status |
|---|---|---|
| Brain | `58c9368` | ✅ Healthy, pulled + restarted |
| Sandbox | `18fe037` | ✅ Synced (pre-quiet-mode commit) |
| Air | `58c9368` | ✅ Pulled |
| Gateway | Pull needed | ⚠️ Not pulled this session |
| Endpoint | Pull needed | ⚠️ Not pulled this session |

---

## Open Items

| # | Item | Priority |
|---|---|---|
| 1 | Child profile RLS — Ryleigh/Sloane DB layer | P1 — security blocker |
| 2 | Service Identity per-node RS256 | P1 — blocks Approval gateway |
| 3 | Approval Gateway T1–T5 | P2 — needs Service Identity |
| 4 | Sandbox git committer identity (jarvissand@...) | P3 — cosmetic |
| 5 | `/v1/approvals/pending` + `/unlock` — still on bare pool.acquire() | P2 |

---

## Tech Debt Open

| TD | Description | Priority |
|---|---|---|
| TD-58 | smoke_lib.sh v2 — load_secrets + migrate remaining 4 smokes | P3 |
| TD-59 | ✅ CLOSED this session | — |

---

## Next Session Plan

1. Pull Gateway + Endpoint to `58c9368`
2. Child profile RLS — Ryleigh/Sloane DB layer (security blocker)
3. Service Identity per-node RS256

---

## Quick Reference

| Item | Path |
|---|---|
| Stage 6 design | `~/jarvis-alpha/docs/STAGE5D_DESIGN.md` (Stage 5d) |
| GUC canonicalize migration | `brain/db/migrations/20260414_130000_guc_canonicalize.sql` |
| Approval RLS migration | `brain/db/migrations/20260414_140000_approval_rls.sql` |
| FORCE RLS migration | `brain/db/migrations/20260414_150000_force_rls_stage6cd.sql` |
| Semantic fix migration | `brain/db/migrations/20260414_160000_fix_semantic_isolation.sql` |
| Migration runner | `scripts/apply_migrations.sh` (VERBOSE=1 for full output) |
| Previous handoff | `~/jarvis-alpha/docs/handoffs/HANDOFF_2026-04-14_02.md` |

---

*Full RLS hardening complete at `58c9368`. Every table in jarvis_alpha is under FORCE RLS. Next: child profile DB-layer enforcement.*
