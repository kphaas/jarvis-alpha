# HANDOFF_2026-04-01_02.md
Session N — jarvis-alpha Platform Foundation

Last commit: b0a584b
Branch: main
Brain alpha service: https://100.64.166.22:8186

---

## What Shipped

### Architecture Decisions Locked
- jarvis-alpha is a multi-workspace AI platform — workspaces bolt on like products
- Workspace URL model: `/admin` (Ken only), `/vault`, `/medical`, `/[slug]`
- Three role types: `platform_admin`, `workspace_admin`, `workspace_user`
- Dashboard: three sections — Timeline, Service Map, Metrics Boards (live pulse + drill-down)
- Cost refresh: 15-minute intervals
- DB: `jarvis_alpha` — fully isolated from jarvis-core, new DB, new pool, new schema
- Cutover plan: alpha up and healthy → jarvis-core LaunchAgents stopped, clean cut
- Python 3.12 locked for all alpha venvs (3.14 incompatible with pydantic-core/PyO3)
- Alpha brain port: 8186 (jarvis-core owns 8182–8185)

### Family Vault — Workspace 1 Spec Locked
- Document storage + AI search (pgvector, all-MiniLM-L6-v2, vector(384))
- Multi-user Q&A against vault contents
- Per-document access controls — DB-layer RLS, not app-layer
- Full audit log: who accessed what document, when, what query
- Child access (Ryleigh, Sloane): `10_PUBLIC` and `20_PROJECTS` only — enforced by RLS policy
- Pipeline: inbox → AI classifies → Ken confirms → live (no skipping for 30_FINANCE+)
- Storage: hot copy on Brain local filesystem, archive to Unraid via NFS

### Classification Tiers (carried from jarvis-core)
| Tier | Kid Access |
|---|---|
| 10_PUBLIC | ✅ Yes |
| 20_PROJECTS | ✅ Yes |
| 30_FINANCE | ❌ No |
| 40_PRIVATE | ❌ No |
| 50_SECRETS | ❌ Never — no AI access ever |

### Alpha-1 — DB Foundation (Brain)
- `jarvis_alpha` Postgres DB created, owned by jarvisbrain
- Schema applied: 9 tables live
  - alpha_users, alpha_workspaces, alpha_workspace_users
  - jarvis_request_log (with 5 indexes)
  - vault_documents, vault_document_permissions, vault_chunks, vault_access_log, vault_pipeline
- pgvector extension loaded on PG16
- HNSW index on vault_chunks.embedding (vector_cosine_ops)
- RLS policy: vault_child_filter on vault_documents

### Alpha-2 — Service Live (Brain)
- brain/db/session.py — real asyncpg pool, SET LOCAL inside transaction per DB call (Q1-A)
- brain/core/secrets.py — thin re-export of get_secret()
- brain/core/config.py — ALPHA_PORT=8186, ALPHA_NODE, ALPHA_VERSION
- brain/middleware/log_middleware.py — every request logged async to jarvis_request_log
- brain/routes/ask.py — /v1/ask stub
- brain/routes/chat.py — /v1/chat stub
- brain/routes/memory.py — /v1/memory stub
- brain/routes/vault.py — /v1/vault/upload, /pipeline, /pipeline/{id}/confirm, /search, /ask stubs
- brain/app.py — middleware order Auth → RLS → RateLimit → Log → route, all routers mounted
- scripts/jarvisalpha_pull.sh — pull-only script for Brain (never pushes)
- scripts/start_alpha_brain.sh — boots on 8186 with Tailscale TLS, python3.12 explicit
- scripts/jarvisalpha_commit.sh — python3.12 -m ruff format explicit
- requirements.txt — 7 pinned packages, all installed on Brain python3.12 venv

### Health Check — Confirmed Live
```
GET https://100.64.166.22:8186/health
{
    "status": "ok",
    "service": "jarvis-alpha-brain",
    "db": "ok",
    "version": "0.1.0"
}
```

---

## Known Issues / Carry Forward

| # | Item | Priority |
|---|---|---|
| 1 | Suppress Keychain -25308 noise in jarvisalpha_pull.sh (add GIT_TERMINAL_PROMPT=0) | Low |
| 2 | Alpha brain has no LaunchAgent yet — must be started manually | P1 next session |
| 3 | jarvis-core #17–29 (KI-1 through KI-13) still open | Various |
| 4 | jarvis-alpha #5 — Forge project_id enforcement | P1 |
| 5 | jarvis-alpha #6 — Chat thread retention cron | P1 |
| 6 | jarvis-alpha #7 — Perplexity validation gaps | P1 |

---

## Next Session Priorities

1. **LaunchAgent for alpha brain** — com.jarvis.alpha.brain plist, loads on login, restarts on crash
2. **Alpha-3 — /v1/ask wired** — real asyncpg query, jarvis_request_log row confirmed on every call
3. **Vault pipeline API** — /v1/vault/upload writes to vault_pipeline stage=inbox
4. **Dashboard design** — wireframe the three sections (Timeline, Service Map, Metrics) before any React

---

## Key Reference

| Item | Value |
|---|---|
| Alpha brain URL | https://100.64.166.22:8186 |
| Alpha DB | jarvis_alpha on Brain Postgres 16 |
| Brain venv | ~/jarvis-alpha/.venv (python3.12) |
| Pull script | bash ~/jarvis-alpha/scripts/jarvisalpha_pull.sh |
| Commit script | bash ~/jarvis-alpha/scripts/jarvisalpha_commit.sh "msg" |
| Boot (manual) | set -a && source ~/jarvis/.secrets && set +a && cd ~/jarvis-alpha && .venv/bin/python3.12 -m uvicorn brain.app:app --host 0.0.0.0 --port 8186 --ssl-certfile ~/jarvis/certs/brain.crt --ssl-keyfile ~/jarvis/certs/brain.key --log-level info |
