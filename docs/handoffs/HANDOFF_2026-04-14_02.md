# JARVIS Alpha — Session Handoff 2026-04-14 #02

**Session focus:** State review → Temporal POC (Sandbox Claude) → GO verdict → TEMPORAL_DECISION.md committed → Brain synced → Stage 5d.2 discovery prompt sent (pending results).

**Status:** Temporal decision locked at `63e34f4`. Brain healthy. Stage 5d.2 discovery running on Sandbox — results not yet reviewed. Next session: review discovery output → execute 5d.2.

---

## Commits Shipped

| # | Commit | What |
|---|---|---|
| 1 | `63e34f4` | docs(temporal): Temporal POC GO verdict — High confidence, all invariants pass |

---

## Temporal POC — CLOSED (GO)

Full POC ran via Sandbox Claude. All phases passed:

| Phase | Result |
|---|---|
| Install | `brew install temporal` — ARM64 bottle, 1.6.2, no Docker |
| Dev server | `SERVING` on 7233/8233, SQLite backing file |
| Python SDK | `temporalio 1.18.2` into existing venv, zero conflicts |
| Workflow POC | End-to-end in 2.30s (0.30s effective overhead), approval signal round-tripped |
| LaunchAgent | Headless confirmed — two standard plists sufficient |

**Verdict: GO / High confidence.** TEMPORAL_DECISION.md at `~/jarvis-alpha/docs/TEMPORAL_DECISION.md`.

### Open Questions Answered (logged in TEMPORAL_DECISION.md)

| Q | Decision |
|---|---|
| Postgres layout | Own `temporal` database on Brain — isolated from `jarvis_alpha` |
| Dev vs full server | Dev server with Postgres persistence — fine for JARVIS scale |
| Namespace strategy | Single `default` namespace — split only when there's a concrete reason |
| Worker count | Single omnibus worker for Alpha-2 |
| R2 drop | Confirmed — `TaskGraphExecutor` deleted in Alpha-2 rewrite |
| First task port | Overnight briefing dispatch — linear, low risk, good reference |
| Metrics | Temporal `/metrics` standalone for now |

### POC Cleanup

- Temporal dev server killed, 7233 free
- `/tmp/temporal_poc.db`, `/tmp/jarvis_temporal_poc.py`, `/tmp/temporal_poc_server.log` removed
- `temporal 1.6.2` binary retained at `/opt/homebrew/bin/temporal`
- `temporalio 1.18.2` retained in `~/jarvis-alpha/.venv`

---

## Node State at Session Close

| Node | Commit | Status |
|---|---|---|
| Brain | `63e34f4` | ✅ Healthy, pulled + restarted |
| Sandbox | `63e34f4` | ✅ Synced |
| Gateway | Manual pull needed | ⚠️ Not pulled this session |
| Endpoint | Manual pull needed | ⚠️ Not pulled this session |

---

## Stage 5d.2 — IN PROGRESS (Discovery Pending)

Scope confirmed (narrowed per Temporal GO):
- **R1 GUC fix** — `executor.py` + `watchdog.py`: `jarvis.role='admin'` → `'platform_admin'`
- **DSN cutover** — `executor.py`: `ALPHA_DB_DSN` → `ALPHA_DB_DSN_EXECUTOR` (new env var)
- **R2 DROPPED** — `TaskGraphExecutor` status values deferred; executor deleted in Alpha-2 rewrite

Discovery prompt sent to Sandbox Claude at session close. Sandbox is running discovery on:
1. All GUC occurrences of `jarvis.role='admin'` in Brain Python
2. Exact lines in `executor.py` and `watchdog_agent.py`
3. Current DSN usage in `executor.py`
4. Existing DSN env vars in `~/jarvis/.secrets`
5. RLS policy check value on Postgres (confirm `'platform_admin'` string)
6. Confirm `watchdog_agent` DSN still correct from 5d.1

**Results not yet pasted. First action next session.**

---

## Next Session Plan

1. **Paste Stage 5d.2 discovery results** — Sandbox Claude should have them ready
2. **Review discovery with Air Claude** — confirm R1 scope, no surprises
3. **Execute Stage 5d.2** — Sandbox Claude executes, Air Claude reviews
4. **Soak 5d.2** — minimum 24h before Stage 6
5. **TD-59** — quick win: `jarvisalpha_pull.sh` silent failure fix

---

## Working Memory

- Latest alpha commit: `63e34f4`
- Stage 5d.1 still soaked and healthy (watchdog_agent on writer, PID 3518 stable)
- Task tables still empty — R2 still dormant
- Temporal binary + SDK installed on Sandbox (not yet on Brain — Alpha-2 concern)
- `TEMPORAL_DECISION.md` is the authoritative GO record

---

## Tech Debt Open

| TD | Description | Priority |
|---|---|---|
| TD-58 | smoke_lib.sh v2 — load_secrets + migrate remaining 4 smokes | P3 |
| TD-59 | jarvisalpha_pull.sh silent failure on git pull error | P2 |

---

## Alpha-2 Remaining (post Stage 5d.2 + 6)

| Deliverable | Status |
|---|---|
| Stage 6 — FORCE RLS remaining tables | ❌ NEXT after 5d soak |
| Child profile RLS (Ryleigh/Sloane) | ❌ Security blocker |
| Service Identity (per-node RS256) | ❌ Blocks Approval |
| Approval Gateway T1–T5 | ❌ Needs Service Identity |
| Dream Mode LaunchAgent + scheduler | ❌ Open |
| Temporal integration (Brain) | ❌ Alpha-2 scope |

---

## Quick Reference

| Item | Path |
|---|---|
| Temporal decision | `~/jarvis-alpha/docs/TEMPORAL_DECISION.md` |
| Stage 5d design | `~/jarvis-alpha/docs/STAGE5D_DESIGN.md` |
| Smoke 5d.1 | `~/jarvis-alpha/scripts/smoke_5d1_watchdog_agent.sh` |
| Smoke task_events | `~/jarvis-alpha/scripts/smoke_task_events_insert.sh` |
| smoke_lib.sh | `~/jarvis-alpha/scripts/smoke_lib.sh` |
| Sandbox Claude runbook | `~/jarvis-alpha/docs/SANDBOX_CLAUDE_RUNBOOK.md` |
| Previous handoff | `~/jarvis-alpha/docs/handoffs/HANDOFF_2026-04-13_01.md` |

---

*Temporal POC GO at `63e34f4`. Stage 5d.2 discovery pending. Review results → execute → soak next.*
