# HANDOFF_2026-04-02_R.md
Session R — TaskGraph Executor, dispatch, tasks routes, buddy recovery

Last commit: 5317675
Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100
Alpha DB: jarvis_alpha on Brain Postgres 16

---

## What Shipped This Session

### 1. Migration 008b — alpha_task_events
File: brain/db/migrations/008b_task_events.sql
- Separate from alpha_buddy_events (UI notifications)
- alpha_task_events: TaskGraph-scoped events only
- Fields: id, event_type, graph_id, step_id, message, priority, read, created_at
- RLS: task_events_isolation policy applied
- Indexes: (read, created_at), graph_id

### 2. TaskGraph Executor
File: brain/tasks/executor.py
- Class: TaskGraphExecutor(db_pool, max_concurrent=3)
- Full DAG resolution — parallel steps via asyncio.gather
- Retry logic: 3 attempts → halted on 3rd failure
- Concurrency cap: asyncio.Semaphore(3)
- Stuck graph recovery: recover_stuck_graphs() resets running→pending on Brain startup
- Writes to alpha_task_events on step_failed, graph_halted
- Intentionally decoupled from FastAPI — extractable to worker node with zero schema changes

### 3. Dispatch Router
File: brain/tasks/dispatch.py
- Routes step.executor → call_tool_agent / call_code_agent / call_memory_agent / call_llm_agent
- All handlers are stubs returning { success: true, output: { result: "stub" } }
- Each handler has WORKER NODE UPGRADE PATH comment
- Unknown executor type returns success=false gracefully

### 4. Tasks Routes
File: brain/routes/tasks.py
- POST /v1/tasks/graphs — create graph
- GET /v1/tasks/graphs — list graphs with step_count
- GET /v1/tasks/graphs/{graph_id} — graph + all steps
- POST /v1/tasks/graphs/{graph_id}/steps — add step
- POST /v1/tasks/graphs/{graph_id}/approve — CI guard + triggers executor
- GET /v1/tasks/graphs/{graph_id}/status — step status summary
- GET /v1/buddy/events — unread alpha_task_events
- Wired into brain/app.py

### 5. buddy.py Recovery
- Cursor incorrectly deleted brain/routes/buddy.py during tasks.py creation
- Recovered from git history: git checkout 87e8b0b -- brain/routes/buddy.py
- buddy.py serves alpha_buddy_events (UI notifications) — separate from alpha_task_events
- buddy_router restored in app.py

### 6. Agent Hierarchy Locked
- Ken → Buddy → Orchestrator → Planner / Executor / Memory / Tool / Code / Overnight agents
- Buddy: creates tasks and notifies, never executes
- Agent-created graphs: ci_required=true, approval gate required
- Ken-created graphs: ci_required=false, runs immediately
- All inter-agent calls logged to agent_steps

---

## Smoke Tests Passed
- POST /v1/tasks/graphs → graph_id returned ✅
- GET /v1/tasks/graphs → graph listed with all fields ✅
- GET /v1/buddy/events → empty array, no error ✅

---

## Known Issues / Next Session

| # | Issue | Priority |
|---|---|---|
| 1 | VITE_BRAIN_TOKEN is 1hr test token — expires | P0 |
| 2 | Need 30-day service token or PIN-based auth flow | P0 |
| 3 | nginx alpha.conf not in git — lives on Endpoint only | P1 |
| 4 | dispatch.py handlers are stubs — need real implementation | P1 |
| 5 | recover_stuck_graphs not wired to Brain startup hook yet | P1 |
| 6 | JWT not wired to jarvis-core endpoints (T36) | P1 |
| 7 | WebSocket for Buddy events — polling only | P1 |
| 8 | /v1/home/summary costs_today_usd always null | P2 |
| 9 | Health page LaunchAgent section not built | P2 |
| 10 | Home page not wired to real data | P2 |
| 11 | Agent hierarchy + inter-agent communication contract not formally documented | P1 |
| 12 | Brain SPOF — alpha shares same Mac Studio, no isolation strategy documented | P2 |
| 13 | Source-aware context compression not built — cost impact grows with Buddy + executor LLM calls | P2 |
| 14 | Child profile RLS (Ryleigh/Sloane) — multi-tenant isolation gap, same fix as KI-13 | P1 |
| 15 | Executor runs in-process — migrate to LaunchAgent when concurrent graph volume exceeds 5 | P3 |
| 16 | Worker node architecture — executor extracts to Postgres-polling worker at Phase 10+ | P3 |
| 17 | jarvis-core KI-1 through KI-13 still open | Various |

---

## Standing Cursor Rules (add to every prompt)
- Do not delete, rename, or modify any file not explicitly mentioned in this prompt
- If you think a file should be removed, add a comment only — do not touch it
- Always verify with wc -l, grep -n after writing files
- Run each verification command and paste raw output — do not describe expected output

---

## Worker Node Upgrade Path (when ready)
- executor.py class requires zero changes
- dispatch.py handlers become HTTP POST to Brain API
- Worker polls alpha_task_graphs for status=pending
- alpha_task_events written by worker unchanged
- Trigger: when concurrent graph volume exceeds 5 regularly

---

## Next Session Priorities (Ordered)
1. Fix VITE_BRAIN_TOKEN — generate 30-day service token
2. Commit nginx alpha.conf to repo + Endpoint deploy step
3. Wire recover_stuck_graphs to Brain startup
4. Implement dispatch.py real handlers (tool + code agents first)
5. WebSocket for Buddy events

---

## Key Reference

| Item | Value |
|---|---|
| Alpha Brain URL | https://jarvis-brain.tail40ed36.ts.net:8186 |
| Alpha UI URL | https://jarvis-endpoint.tail40ed36.ts.net:4100 |
| Alpha DB | jarvis_alpha on Brain Postgres 16 |
| Brain venv | ~/jarvis-alpha/.venv (python3.12) |
| Commit script | bash ~/jarvis-alpha/scripts/jarvisalpha_commit.sh "msg" |
| Pull script | bash ~/jarvis-alpha/scripts/jarvisalpha_pull.sh |
| Restart alpha | bash ~/jarvis-alpha/scripts/restart_alpha.sh |
| Health check | bash ~/jarvis-alpha/scripts/check_alpha.sh |
| Gen test token | python3 ~/jarvis-alpha/scripts/gen_test_token.py |
| JWT private key | ~/jarvis/pki/jwt/jwt_private.pem |
| JWT public key | ~/jarvis/pki/jwt/jwt_public.pem |
| Alpha public key | ~/jarvis-alpha/brain/pki/jwt_public.pem |
| Secrets (Brain) | ~/.secrets |
| nginx config (Endpoint) | /opt/homebrew/etc/nginx/servers/alpha.conf |
| Buddy log | ~/jarvis-alpha/logs/alpha_buddy.log |
| Migrations dir | ~/jarvis-alpha/brain/db/migrations/ |
| Models config | ~/jarvis-alpha/brain/core/models.py |
| Tasks executor | ~/jarvis-alpha/brain/tasks/executor.py |
| Tasks dispatch | ~/jarvis-alpha/brain/tasks/dispatch.py |
| Tasks routes | ~/jarvis-alpha/brain/routes/tasks.py |
