# HANDOFF_2026-04-06_01.md
Session — Approval Gateway Phase 2 Complete + Docs + Infra Hardening

Branch: main
Alpha Brain: https://jarvis-brain.tail40ed36.ts.net:8186
Alpha UI: https://jarvis-endpoint.tail40ed36.ts.net:4100

---

## What Shipped This Session (18 Deliverables)

### 1. Schema Fix — alpha_buddy_events
- Migration 011: added `source` (TEXT) and `payload` (JSONB) columns
- rotate_service_token.py updated: event_type='alert', source='token_rotation'
- Removed references to non-existent `severity` column

### 2. Route Classification — Security/Mesh/Logs
- `GET /v1/security/*` → security_read → T2 (audited)
- `GET /v1/mesh/certs` → read → T1
- `GET /v1/logs/query` → security_read → T2 (audited)
- Perplexity-validated: log endpoints bumped to T2 due to potential PII in logs

### 3. Approval Middleware — T4/T5 Queue + 403 Response
- T4/T5 requests INSERT into alpha_approval_queue → return 403 with queue_id, tier, action_class
- Queue INSERT is BLOCKING (not fire-and-forget)
- Audit INSERT in same transaction
- Body only consumed for T4/T5 — T1-T3 pass through untouched (fixes BaseHTTPMiddleware hang)

### 4. Schema Alignment — Approval Middleware
- Fixed 10+ column mismatches between middleware code and actual table schemas
- Queue: id is UUID (not prefixed text), requested_at (not created_at), risk_tier (not tier)
- Audit: approval_id FK (not queue_id), decision column (not action)
- All NOT NULL columns now provided: actor_type, description, nonce

### 5. Pull Script Hardened Restart
- scripts/jarvisalpha_pull.sh now: unload LaunchAgent → kill port 8186 → purge __pycache__ → load LaunchAgent → health check
- Health check output: "✅ Alpha Brain healthy after restart" or error log tail
- Fixes recurring issue of stale processes holding port

### 6. Approval Routes — unlock, pending, decide
- File: brain/routes/approvals.py (223 lines)
- POST /v1/approvals/unlock — PIN validates → 5-min approval JWT with purpose="approval"
- GET /v1/approvals/pending — lists pending queue items
- POST /v1/approvals/{queue_id}/decide — approve/deny with X-Approval-Token header
- Mounted in app.py, classified as T2 (prevents approval-of-approval deadlock)

### 7. PIN Re-Entry — 5-Min Approval Window
- Separate JWT with purpose="approval" claim, 300s TTL
- Validates against same PIN auth logic as main login
- X-Approval-Token header required on decide endpoint
- Expired/invalid token → 403 → UI shows PIN modal again

### 8. Mark-and-Retry with 10-Min TTL
- On approve: status='approved', new expires_at = NOW() + 10 minutes
- Client retries original request
- Middleware checks for approved + unexpired row → passes through → marks 'executed'
- No stored request replay — stateless design

### 9. Dedup Handling
- Partial unique index idx_approval_pending_dedup catches duplicate (actor_sub, parameters_hash)
- Middleware catches UniqueViolationError → returns existing pending queue_id
- No more 500 on duplicate T4/T5 requests

### 10. Body Consumption Fix
- Restructured dispatch: classification happens BEFORE body read
- T1/T2/T3 use original request with call_next — no body consumption
- T4/T5 uses request.body() (Starlette cached reader) — safe because we return JSON
- Removed receive_body/new_receive/replay_request pattern entirely

### 11. Rich Approval Notifications
- File: brain/services/approval_notifier.py (157 lines)
- build_notification() generates rich context: tier explanation, risk detail per action class, if-approved/if-denied consequences
- send_approval_notification() logs + writes buddy event
- Phase 3 stub for Pushover via Gateway
- JSONB payload serialized with json.dumps()

### 12. Comprehensive Route Classification
- 142 routes classified in approval_classes.py
- Zero unclassified routes on startup audit
- Covers: all metrics, security panel, logs, tasks, threads, memory, vault, costs, dream, MCP, honeypot traps, FastAPI docs

### 13. Startup Route Audit + CI Test
- brain/middleware/approval_audit.py — scans all mounted routes on startup
- Logs ROUTE_AUDIT WARNING with unclassified list, or INFO if all classified
- tests/test_route_classification.py — CI gate, fails if any route unclassified

### 14. PATTERNS.md
- docs/PATTERNS.md — engineering gotchas reference
- Covers: asyncpg JSONB/TEXT[], ASGI body caching, LaunchAgent restart, secrets, inter-node comms, route classification, debugging

### 15. Approvals UI
- Full rewrite of ui/src/pages/Approvals.tsx (330 lines)
- Fetches from /v1/approvals/pending (not old TaskGraph endpoint)
- PIN modal with 5-min unlock
- Risk context cards with tier badges, action class badges, countdown timer
- Approve/deny with X-Approval-Token header
- Success/failure toast, locked/unlocked state indicator

### 16. JSONB Serialization Fix
- approval_notifier.py: payload must be json.dumps() for asyncpg JSONB columns
- Documented in PATTERNS.md as Pattern #1

### 17. DB_CONTRACTS.md
- docs/DB_CONTRACTS.md — every table's schema, constraints, and correct INSERT examples
- Covers 11 core tables with exact code patterns
- Universal rules section: JSONB, TEXT[], UUID, CHECK constraints

### 18. SERVICE_CONTRACTS.md
- docs/SERVICE_CONTRACTS.md — inter-service communication reference
- Covers: approval lifecycle, buddy events, notifications, PIN auth, service tokens, route classification, memory system, inter-node communication, forge integration, UI patterns

---

## Files Created This Session

- brain/migrations/011_buddy_events_columns.sql
- brain/routes/approvals.py (223 lines)
- brain/services/approval_notifier.py (157 lines)
- brain/middleware/approval_audit.py (42 lines)
- tests/test_route_classification.py (17 lines)
- docs/PATTERNS.md
- docs/DB_CONTRACTS.md
- docs/SERVICE_CONTRACTS.md

## Files Modified This Session

- brain/middleware/approval.py (282 lines — major restructure)
- brain/middleware/approval_classes.py (142 routes classified)
- brain/app.py (approvals router mount + audit call)
- scripts/jarvisalpha_pull.sh (hardened restart)
- scripts/rotate_service_token.py (event_type fix)
- ui/src/pages/Approvals.tsx (full rewrite)

---

## Alpha-2 Status — COMPLETE

| Deliverable | Status |
|---|---|
| asyncpg pool | ✅ COMPLETE |
| TaskGraph DAG schema | ✅ COMPLETE |
| TaskGraph executor | ✅ COMPLETE |
| Kill switch | ✅ COMPLETE |
| Circuit breaker + watchdog | ✅ COMPLETE |
| Service Identity Phase 1 | ✅ COMPLETE |
| Service token auto-rotation | ✅ COMPLETE |
| Scope enforcement | ✅ COMPLETE |
| Child profile auth | ✅ COMPLETE |
| Child RLS enforcement | ✅ COMPLETE |
| RLS wired to chat/ask/tasks/dream | ✅ COMPLETE |
| RLS gap fix | ✅ COMPLETE |
| Approval Gateway — schema + T1-T3 | ✅ COMPLETE |
| Approval Gateway — T4/T5 queue + decide + UI | ✅ COMPLETE (this session) |
| Rich notifications | ✅ COMPLETE (this session) |
| Route classification (142 routes, zero gaps) | ✅ COMPLETE (this session) |
| Startup route audit + CI test | ✅ COMPLETE (this session) |
| Engineering docs (PATTERNS + DB_CONTRACTS + SERVICE_CONTRACTS) | ✅ COMPLETE (this session) |

**Alpha-2 is done.** All planned deliverables shipped.

---

## Known Issues

- Approval queue expiry cleanup job not built — expired pending rows stay in table until manually deleted
- notification_sent flag in alpha_approval_queue not updated by notifier (cosmetic — notifications work via buddy events)
- Pushover delivery is stub only — wires to log + buddy event, not mobile push
- Service tokens expire April 12 — auto-rotation LaunchAgents will fire ~April 10
- UI best practices backlog: component extraction, custom hooks, lazy loading, react-query (noted but not blocking)

---

## Next Session Priorities (Alpha-3)

| # | Task | Est |
|---|---|---|
| 1 | Approval queue expiry cleanup job (Buddy agent or LaunchAgent) | 30 min |
| 2 | Pushover wiring — Gateway /v1/notify/pushover endpoint | 1 session |
| 3 | Dream Mode — overnight loop + morning briefing | 2-3 sessions |
| 4 | Voice UI port from jarvis-core | 1-2 sessions |
| 5 | UI refactor — component extraction, custom hooks, lazy loading | 1 session |
| 6 | Startup route audit → CI enforcement in GitHub Actions | 30 min |

---

## Quick Reference

| Item | Value |
|---|---|
| Approval routes | brain/routes/approvals.py |
| Approval middleware | brain/middleware/approval.py |
| Classification registry | brain/middleware/approval_classes.py |
| Route audit | brain/middleware/approval_audit.py |
| Notifier | brain/services/approval_notifier.py |
| Approvals UI | ui/src/pages/Approvals.tsx |
| Pull script | scripts/jarvisalpha_pull.sh |
| CI test | tests/test_route_classification.py |
| PATTERNS doc | docs/PATTERNS.md |
| DB contracts doc | docs/DB_CONTRACTS.md |
| Service contracts doc | docs/SERVICE_CONTRACTS.md |
| Migration 011 | brain/migrations/011_buddy_events_columns.sql |
| Middleware stack | CORS → Auth → RLS → Approval → RateLimit → Log → handler |

---

*JARVIS Alpha — Approval Gateway Phase 2 Complete · Alpha-2 DONE · April 6, 2026*
