.secrets
*.key
*.pem
!*_public.pem
*.crt
__pycache__/
*.pyc
.venv/
node_modules/
dist/
.env
*.db
.DS_Store
.cursor_tmp_venv/
*_tmp_venv/

# PKI — private keys never in git
*_private.pem

# Logs
logs/
*.log

# Backups
*.backup-*

# Vault storage
vault_storage/

# Python egg-info
*.egg-info/

# Script backups — local only, never committed
scripts/*.bak_*
scripts/*.bak

# TD-108 validation — verify skip renderer (Apr 18)

# Rendered launchd plists — generated by scripts/install_launchagents.py
# Templates (.template.plist) ARE tracked; rendered output is ephemeral
launchagents/*.plist
!launchagents/*.template.plist

# Temporal UI — rendered from config.yaml.template at startup
brain/config/temporal-ui/config.yaml
